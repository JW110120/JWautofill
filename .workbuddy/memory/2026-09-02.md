# 2026-09-02

## 通用组件 CSS 收敛（单一来源）
- 新建 `src/styles/common.css` 收口全插件通用组件样式（滑块文字标签 / range-slider / number-input+单位 / 图标按钮 / 普通按钮族 / 开关行 / 双列 radio / 双列+单列 checkbox / 拖拽光标锁），每组件只定义一次，子面板/工具箱类名作别名挂同一条规则。
- `index.tsx` 顶部最先 `import './styles/common.css'`；各面板 CSS 清空通用副本，仅留面板独有布局：
  - `styles.css`：删 `.main-button-container` 占位（归 common）。
  - `pattern.css`：删 `.icon-button`（死代码，全用 .hotkey-icon-button）、`.close-button`、`.pattern-slider-parameter-collection` 作用域块、`.pattern-slider-text` 档、`.pattern-angle-range/.pattern-scale-range`、重复 `body.label-drag-cursor`；保留 `.pattern-setting-item` 通配 + 单位裸 `<span>` 样式（图案用裸 span 非 .num-unit）+ 35px number-input 覆盖。
  - `stroke.css`：删 `.stroke-label-2/4` 整块（改留 margin+拖拽光标）、`.num-input-*`/`num-unit` 重复、`.position-radio-item`、重复 `body.label-drag-cursor`；保留容器布局 + `.stroke-blende-mode label` + 35px 覆盖。
  - `adjustment.css`：删 `.adjustment-button` 族 / `.hotkey-icon-button` / `.adjustment-swtich-*` / `.adjustment-slider-label(-3/-4)` / `.adjustment-unit` / `body.label-drag-cursor`；保留工具箱独有（分区/模态/蒙版同步/笔刷热键/#pixeladjustment number-input 35px/.adjustment-slider-label-drag|-static/.wide-/.wider-adjustment-slider-label）。
- 删 `src/components/CustomSwitch.tsx`（仅自引用，PS 新版原生 sp-switch）。
- 关键 cascade 修复：`common.css` 滑块标签 10px 规则加 `:not(.adjustment-slider-label-static)`，否则下拉行标签 6px 被 0,2,0 压掉；`.stroke-label-2/4`、`.wide/.wider-adjustment-slider-label` 移出 common 宽度档避免裁切文字。
- 验证：`webpack --mode=production` 0 TS 错误；bundle 中 dead `.icon-button` 与 `CustomSwitch` 残留均为 0。

## 收尾复核（续）
- 复核 `.adjustment-button-lv2/-wide/-5..-8/-quad` 宽度分档：实际规则此前已删，adjustment.css 仅剩 line 574「↳ 已收口到 common.css」注释标记（line 564 同款 .adjustment-unit 标记）；common.css 581-593 完整持有全部分档（46/85/98/111/124/124/92px）。单来源成立。
- 复核 adjustment.css 现状：`.adjustment-button`/`.adjustment-swtich-*`/`.adjustment-slider-item`/`.adjustment-slider-label(-3/-4)`/`.adjustment-unit`/`body.label-drag-cursor` 均已移除，仅留 `.adjustment-slider-label-drag|-static`（工具箱独有拖拽光标）与注释标记。
- 二次 `webpack --mode=production` 通过（仅体积 warning，无 error），adjustment.css 41KiB 合法编译，common.css 首行 import 生效。
- MEMORY.md 校正：「布局宽度」按钮宽度落点改为 common.css；新增「通用组件 CSS 单一来源」定稿小节（单一来源/收口清单/级联陷阱/命名约定）。

## 子面板外壳收口（用户采纳全部建议）
- 新增 common.css「8d. 子面板外壳」：容器 .adjustment-container/.color-settings-panel/.gradient-picker/.strokesetting/.pattern-picker 逗号选择器统一 `padding:10+box-sizing+overflow-y/x:auto` + 自定义滚动条(thumb/track)；标题栏 `.panel-header`→`.subpanel-header`（改名避免与大面板标题混淆）。
- 4 个 TSX 改名 className="panel-header"→"subpanel-header"：ColorSettingsPanel:252/StrokeSetting:102/PatternPicker:1641/GradientPicker:1271。
- 各面板 CSS 删外壳副本：colorpanel.css(.color-settings-panel 去 --adjust-scrollbar-w/padding/box-sizing/overflow + .panel-header 规则)、gradient.css(.gradient-picker 同上)、stroke.css(.strokesetting 同上 + 删 `.strokesetting .panel-header` 作用域覆盖，不再特殊处理)、pattern.css(.pattern-picker padding:8→统一10)、adjustment.css(.adjustment-container 去外壳+整块 ::-webkit-scrollbar)。各自留「↳ 已收口到 common.css」注释标记。
- **关键漂移点**：styles.css 原 `#app .strokesetting/.color-settings-panel/.gradient-picker/.pattern-picker::-webkit-scrollbar` 带 #app id 选择器(1,1,1)会压过 common 无作用域(0,1,1)，且子面板确认也会在 #app 渲染(app.tsx:1804-1837) → 已删这 4 个 #app 作用域子面板滚动条选择器，仅留 `#app .container` 主面板（大面板不在子面板收口范围）。否则改 common 对 #app 下的子面板不生效。
- 三次 `webpack --mode=production` 通过（仅体积 warning）；verify：`.panel-header` 类规则已无残留（仅注释），子面板滚动条仅 common.css + `#app .container` 两处、单一来源成立。

## 子面板 CSS 二次收敛（用户指正"只留真正独有"，补做属性级冗余）
- 用户指正：首轮只收口组件级，子面板 CSS 仍残留与 common 重复的属性级冗余（外壳 position/width/height、逐面板 35px 数字输入等）。
- 核实关键事实：① common.css 原**无**原生 `select` 样式、滑块数字输入是 24px 而非 35px → 用户"common 已收口"对 gradient-setting-item 举例**不确切**；② 全项目 TSX **无原生 `<select>`**（下拉统一走 common `.mask-sync-select-wrap`）→ `.gradient-setting-item select` 是**死规则**，已删；③ 另有 src/styles/input-fix.css（`@import` 进 styles.css，manifest 全插件生效）持功能性可见/遮挡规则，且用 `!important` 强制 4 子面板 `z-index:9999` → 子面板 CSS 里 z-index 副本冗余。
- common.css 扩展子面板外壳块：新增 4 子面板 `position/width/height:calc(100%-20px)/background/flex列/justify` + `.xxx > *{flex-shrink:0}`；新增 35px 设置项数字输入单一来源（`.gradient-setting-item input`/`.pattern-setting-item input`，与工具箱 35px 不同在于无 border/flex/font-size:11，刻意保留差异）。
- 删除：colorpanel/stroke/gradient/pattern 4 文件的外壳布局属性 + `> *` flex-shrink:0（z-index 由 input-fix.css 强制，子面板里副本删）；gradient 死 select 规则；gradient/pattern 逐字相同的 35px 数字输入（已收口 common）。
- **刻意不动**：input-fix.css 功能性可见/遮挡规则（popOverlay 弹层逻辑依赖、注释禁改 !important）；工具箱 `.adjustment-number-input`(35px 带 border/flex/font-size:11) 与 `#pixeladjustment input`(22px高) 属工具箱独有；`.opacity-input input`(gradient 35px 但 height:24/padding 不同) 留 gradient 独有。
- 四次 `webpack --mode=production` 通过；verify：`.gradient-setting-item select` 零残留；35px 数字输入仅 common.css:667-668 一处；子面板 `position:absolute` 仅 common.css:644（其余为内部元素）。

## 第三轮：子面板逐条审计 + 35px→24px 定稿（17:00-17:30）
- 用户定稿：子面板数字宽度一律按 common 24px，35px 全删；子面板 CSS 只留真独有组件/容器；通用件 TSX 重新引用 common 类名。
- 删：common.css 设置项 35px 规则；gradient.css `.opacity-input input`（35px+spinner）；pattern.css `.pattern-setting-item span`/14px 单位容器/`.pattern-fillmode-radio` 本体/`.pattern-checkbox-input`/`.pattern-checkbox-label`/`.field-row-two` 对齐/重复 `.photo-container.selected`；gradient.css `.gradient-setting-item label`（12px）与 `> div > span` 单位规则；stroke.css `.stroke-blende-mode label` 本体（只留 margin-right:6px）；colorpanel `.calculation-mode-radio` 只留 flex:1。
- TSX 改类名：gradient °/% → `.num-unit`（% 由 label 改 span）；pattern °/% → `.num-unit`；gradient「样式/角度/不透明度/颜色」标签 → `.slider-text`；stroke「混合模式」label → `.slider-text`。
- 保留（真独有）：`.preset-item` 35px 预设格、渐变/图案预设容器、色标滑块/中点/取色方块、`.gradient-subtitle h3` 区块标题、`.opacity-input/.color-input-container .delete-button-wrapper{margin-left:auto}`（space-between 行右对齐机制）、pattern checkbox 紧凑覆盖。
- 工具箱 35px（#pixeladjustment）不在子面板范围未动，待用户确认是否也收 24px。
- 构建 `webpack --mode=production` 通过（仅体积 warning）；验证：子面板 CSS 无 35px 数字输入、无裸 °/%、`gradient-subtitle` 仅剩两处 h3 区块标题。

## 第四轮：工具箱跟进 + 通用件零副本定稿（17:40-18:10）
- 用户定稿：子面板与工具箱 CSS 凡 common 已有的通用件一律删掉、TSX 连 common 类名；漂移=改漏，按 common 对齐；工具箱跟进相同操作。
- common.css 新增：可拖拽标签别名组（.draggable-label+.adjustment-slider-label-drag：ew-resize+user-select:none+dragging/not-dragging）；下拉行标签 6px 规则（.adjustment-slider-label-static+.gradient-setting-item>label+.stroke-blende-mode label）；描边行标签 10px（.stroke-wide-container/.stroke-opacity-control .slider-text）；两列 radio 行布局块（三容器前缀 sp-radio-group space-around / sp-radio flex:1 space-between）；radio-item-label 补 font-size:13px+color；input[type=number] appearance:none 去作用域为全局单条；移除 .stroke-label-2/-4 别名。
- pattern.css：删 .pattern-checkbox-container 紧凑覆盖（padding:5px 8px 2px/margin-bottom:5px）与 .field-cell-label 不撑满覆盖（吃 common padding:10/height:100）、.pattern-fillmode-container sp-radio-group/sp-radio 布局（留 margin:-8px 顶补偿与 105px 间距）、.pattern-fillmode-radio span{margin-top:20px}。
- stroke.css：删 .stroke-label-2/-4 全部块（TSX 改 slider-text+draggable-label）、.position-radio-group sp-radio 布局（留 45px）、.stroke-blende-mode label 6px。
- colorpanel.css：删 calculation sp-radio 布局（留 margin-left:5px 与 110px）、.calculation-mode-radio{flex:1}、.calculation-mode-radio .radio-item-label。
- gradient.css：删 .draggable-label 本体、.gradient-setting-item>label 6px。
- adjustment.css：删 .adjustment-slider-label-drag(+dragging)、.adjustment-slider-label-static、.wide/.wider-adjustment-slider-label（50/60px 固定 basis，走 common 内容宽）、#pixeladjustment .num-input-row(+focus-within+input 35×32)、.adjustment-number-input 35px 覆盖、#pixeladjustment input[type=number]（22px 高+border）——工具箱数字输入统一 24px。
- StrokeSetting.tsx 两处标签 stroke-label-2/-4 → slider-text draggable-label。
- input-fix.css #app input z-index:1!important 为功能性，未动。
- 构建 webpack --mode=production 通过（仅体积 warning）；grep 验证：stroke-label/draggable-label 本体/static/drag/number-input 覆盖/pattern checkbox 覆盖零残留，仅 common.css 本体+注释标记+功能性选择器。
- MEMORY.md 全文收敛重写（原超限截断）。
