# 2026-08-28 工作日志

## Hotkey 守护进程相关修复（跨机器拉取后 5 个问题）

1. **ws 报错**：`src/hotkey/HotkeyBridge.ts` 已用 `globalThis.WebSocket` 替代 `require('ws')`（之前 webpack 报 Can't resolve 'ws'）。构建已验证无该 warning。该修复是**未提交**状态——必须 commit/push，否则换机器拉取仍会拿到旧版报错。
2. **录制组合键录不了（根因已修）**：`src/hotkey/BrushHotkeySection.tsx` 的 keydown 监听原本挂在 `window` 上，UXP 面板里 `window` keydown 不触发；改为挂 `document`（与 AdjustmentPanel 一致）。另：笔刷选择用 picker 为主（不再强求输入笔刷名），加了「刷新笔刷」按钮；录制按钮在未选笔刷时禁用。
3. **.gitignore**：新增 `native/HotkeyDaemon/bin/`、`obj/`、`*.user`、`*.suo`、`.vs/`、`*.pdb`、`*.tsbuildinfo`、`*.log` 等；并用 `git rm --cached` 把已提交的 214 个 bin/obj 文件移出索引（磁盘保留）。
4. **安装 UX**：移除「手动输入 exe 路径」文本框，改为按钮式：选择 exe / 运行安装器(install.bat) / 启动 / 卸载守护进程。「运行安装器」用 `storage.localFileSystem.getPluginFolder()` + 逐层 `getEntry` 定位 `native/HotkeyDaemon/install.bat`（兼容不同 UXP 版本），再 `shell.openPath` 运行。
5. **卸载工具**：新建 `native/HotkeyDaemon/uninstall.ps1` + `uninstall.bat`。安全反向 install.ps1：仅按精确进程名停进程、仅删我们写的 HKCU\Run 项、仅删 `%LOCALAPPDATA%\JWautofill\daemon`（带父目录/leaf 校验防误删）、清理临时发布目录、可选删 hotkeys.json（询问）。**脚本必须 ASCII-only**——含中文的 .ps1 在中文 Windows 下会被当 GBK 读导致语法错误（已踩坑，改英文）。
