# 2026-09-18

## 分块平均「加权模式」需求澄清 + 改为自适应对比压缩方案（待用户确认，未改代码）
用户澄清概念：**「分块」= 不连通选区各自独立平均**（因为 PS 原生平均滤镜会把不连通选区的像素混在一起算），
所以普通模式已经正确，不是「按色块分块」。新模式的目标是：各不连通选区**分别减弱自身对比**——
与选区均值接近的大面积像素小幅趋近均值，与均值差距大的小面积特征（细线）大幅趋近均值。

### 关键设计结论
- **判据是「与选区均值的差」，不是面积**。面积只是相关现象（细线既小又偏离大）。面积驱动会反向（大块权重高→被更狠地拉平）。
- 均匀降对比 = 线性缩放（out = m + d·c）**做不到**这个诉求，必须用 |d| 的非线性映射。
- 推荐公式：`k = s·(u/τ)^p/(1+(u/τ)^p)`，`u = |亮度 − 亮度均值|`，`τ = max(σ_L, 6)`，
  `输出 = m + (原值−m)·(1−k)`，k 统一作用于 RGBA（保色相）。移除原 16 簇颜色聚类。
- **单调性约束（重要）**：映射单调 ⟺ `1 − k − u·k' ≥ 0`。p=1 恒单调；p=1.5 仅在 u≈1.59τ 处有 <1.1 级反转；
  **p≥2 会让线条中心反转**（实测最深 -38.3 → -5.7，比邻域 -8.0 还浅 → 线条发虚/空心）。
  p→∞ 的极限（k→1，最远像素完全变均值）必然反转，**「线条完全消失」与「单调无反转」不可兼得**。
- 推荐 **p = 1.5、τ = σ_L**：实测背景段移动 22.8%（绝对约 2 级）、线条段移动 76.3%（绝对约 20 级），
  区分度 3.3×；p=1 只有 2.1×；p=2 达 4.8× 但反转 2.3 级。
- 滑块 s = 强度/10（1–10），建议默认 8 → 背景移动 18%、线条移动 61%。

### 用户这张截图的实测基线
选区均值 RGB(239.2,239.6,242.2)、亮度均值 239.8、σ_L = 15.99；
|d| 分布：<12 占 77%（背景）、>24 占 13.3%（两根粗线）、12–24 仅 9.8%（抗锯齿过渡带）→ 背景/线条在 |d| 上天然可分。
RGB 欧氏 RMS = 26.76 ≈ √3×15.99 → 偏离几乎全在明度轴上，用亮度做判据足够且顺带保色相。

### 实现要点
每分量 2 趟：趟1 求 ΣRGB / ΣL / ΣL²（得均值与 σ），趟2 按 k 写回；`k_eff = (coeff/255) × s × k`。
顺带消灭现有加权分支三个 bug（|0 截断下漂、16 簇封顶丢像素、Uint16 簇计数回卷），且比现方案更快（去掉 O(16N) 内层）。
脚本落在 `outputs/blockAverage-sample-stats.cjs`、`blockAverage-adaptive-eval.cjs`、`blockAverage-profile.cjs`。

## 参数语义定稿：强度 1–10 线性、p 是形状（用户确认需求）
- **k 的语义 = 该像素原始偏离被抹掉的比例**（k=0 不动，k=1 变成选区均值）。`k` 的上界 = `s = 强度/10`
  → 强度10 = 最偏离像素完全贴到均值；强度1 = 最多抹 10%。
  `∂out/∂s = −u·g(u) ≤ 0` ⇒ **强度对每一个像素都严格单调**，1–10 不存在「越调越弱」的像素。
- **强度 1→10 对平均改动幅度严格线性**（本图实测）：每升一级线条段多压 2.82 级（36.37→33.55→…→8.17）、
  背景段多压 0.20 级（7.15→6.95→…→5.15）。
- ⚠️ **严格单调的解析界：`s ≤ 4p/(p+1)²`**（由 `1 − s·(g + u·g') ≥ 0` 推得）。
  p=1→1.000、1.25→0.988、**1.5→0.960**、1.75→0.926、2→0.889。即 s=1 只有 p=1 才严格单调；
  p=1.5 在强度10 有 **1.19 级**反转（u > 1.59τ 起），p=1.5 的 1–9 级全严格单调。
- p 的取舍（强度10，本图）：p=1 背景压 34%/线条压 70%（选择比 2.06、反转 0）；
  **p=1.5 → 28%/78%（2.79、1.19 级）**；p=2 → 23%/84%（3.65、3.99 级）。
- 传输曲线（p=1.5，τ=σ=15.99）：等级1/3/5/7/10 在 u=2→2.0/2.0/2.0/1.9/1.9；u=12→11.5/10.6/9.6/8.7/7.3；
  u=56→51.1/41.4/31.7/22.0/7.4。
- ⚠️ 修正上一节：「p=1.5 反转 0.4 级」是按单条横截面估的；按**全图线条带**统计应为 **1.19 级**，以本节为准。
- ⚠️ 度量陷阱：扫描网格上限外扩到 4× 数据最大值会让「是否单调/反转量」算出假值（0.00 被报成 37.62）。
  凡 `max − end` 型指标，先断言定义域两端一致。
- 新脚本 `outputs/blockAverage-level-sweep.cjs`（等级×改动幅度）、`blockAverage-p-sweep.cjs`（p 取舍 + 传输曲线）。

## 「对比减弱」定稿并落地代码
用户四项决定：判据=差值驱动；命名=**对比减弱 + 强度**；**p 固定 = 1 且不暴露**；默认强度 = 8。
并补充**硬条件：压缩后像素偏离的大小关系不能变**（即映射必须严格单调）——这正是弃用 p=1.5/2 的原因。

### 标定：s = 强度 × 0.07
按用户「把目前的强度 7 映射为 10」：旧强度 7 的 k 上限是 0.7，故新刻度 强度1 → k 上限 7%、**强度10 → 70%**。
⚠️ 换 p 之后「k 上限相等」≠「分档百分比相等」：新强度10 实线条压 49%，旧 p=1.5 强度7 是 54%，
差在 p=1 的选择性弱于 1.5。若真要复刻百分比，系数应取 0.0777，但语义锚点会变模糊，故取 0.07。

### 严格单调性（已解析 + 数值双证）
p=1：`out(u) = u·(1 − s·t/(1+t))`，`d(out)/du ∝ 1 + (1−s)(2t + t²) > 0` 对 `s ≤ 1` 恒成立
→ 任何强度档位都严格单调，**无反转**（实测 1-10 档、20 万点网格，逆序点全为 0）。即使把上限外推到 s=1 也仍是 0。
对照 p=1.5：s=1 时 out(60)=7.25 < out(20)=8.34，正是用户否掉的那种反转。

### 实测（本图 σ_L=15.99、τ=15.99、背景带 7.15 级、线条带 36.37 级）
| 强度 | k 上限 | 背景带 | 线条带 | 最深像素 59.5 |
|---|---|---|---|---|
| 1 | 0.07 | 98% | 95% | →56.2 |
| 8（默认） | 0.56 | 81%（丢 19%） | 61%（丢 39%） | →33.2 |
| 10 | 0.70 | 76%（丢 24%） | 51%（丢 49%） | →26.7 |
每升一级线条带多压约 1.78 级、背景带多压约 0.17 级，严格线性。

### 代码改动（四处）
1. `src/adjustments/blockAverageProcessor.ts`：整个 16 簇颜色聚类分支删除，换成两趟自适应压缩
   （趟1 累计 ΣRGB/ΣA/ΣL/ΣL² → 均值与 σ；趟2 按 k 写回）。顺带消灭 |0 截断下漂、簇满丢像素、Uint16 计数回卷三个 bug。
   签名改名：`useWeightedAverage`→`useContrastReduction`、`weightedIntensity`→`contrastReductionIntensity`（默认 8）。
2. `src/adjustments/AdjustmentPanel.tsx`：状态/处理器/滑块注册与 switch/持久化/复位全量改名；UI 文案「加权模式」→「对比减弱」；
   默认强度 5→8。布局未动（沿用 `label-4` 与 `label-2`，滑块可见标签仍是「强度」二字）。
3. `src/constants/helpTexts.ts`：`weightedMode`→`contrastReduce`、`weightedIntensity`→`contrastReductionIntensity`；
   顺手改掉「按网格对选区分块做加权平均 / 中心权重更高」这两句与实现不符的描述。
4. `src/utils/PanelStateManager.ts`：类型键同步改名。
⚠️ 旧 `panel-state.json` 的旧键**不做迁移**：参数语义已变，沿用旧值会得到偏差力度，回落到新默认（开 + 8）才符合预期。
验证脚本 `outputs/blockAverage-contrast-reduce-verify.cjs`（对照表 + 单调性断言 + 传输曲线）。

## 「对比减弱」新增混合颜色带柔化（batchPlay 方案被否）
用户想加「类似 PS 混合颜色带」的功能，让高强度减弱更温和。原计划 batchPlay 七步：存选区→复制图层→全文档减弱→
按选区均值设 blendRange→向下合并→载回选区→删通道。**该路线整体否掉**，三个死结：

1. **复制 + 向下合并必然抬 alpha**：两层同 alpha=a、上层不透明度 α 时合并 alpha = a(1+α−aα/255) ≥ a。半透明选区必坏。
2. **选区外像素也会被改**：第 3 步无选区 → 按**全文档均值**压缩全文档；第 4 步掩模只按「与选区均值的距离」部分还原。
   实测（均值 240、σ≈16、强度10）：选区外灰值 100 → **142**（三通道斜坡相乘）/ **169**（仅灰色通道），抬亮 42–69 级。
3. **掩模强度取决于均值在色阶中的位置**：斜坡端点是 0/255，均值 240 时亮侧斜坡只有 15 级宽 → 背景噪声近一半落在陡坡上被保护，
   暗侧斜坡 240 级宽 → 线条几乎不被保护。实测背景压缩 24%→5%、线条 49%→38%。

### 像素域等价式（已采纳）
batchPlay 的 RGB 结果 = `out = m + d·(1 − w·k·φ)`，φ 即混合颜色带遮罩；**alpha 不乘 φ** → 正好满足「alpha 只吃对比减弱的平均」。
整条流程塌缩成现有的一次 putPixels，不需要临时通道 / 复制层 / 合并 / 载回选区。
- φ 用**单亮度标量**（三通道相乘其实也退化成标量，同样不漂色相，但单亮度版对明暗两侧对称）。
- ⚠️ **平台段是必需的**：若照原参数把四个手柄全收到均值（平台宽 0），背景压缩会从 24% 掉到 5%，且强度随选区均值位置漂移。
  加平台后**背景分档保留率恒为 76%，与无柔化完全一致**（柔化只作用于 >1.5σ 的特征段）。

### 方向取舍由硬条件裁决
- R1「保护偏离大的像素」（=用户描述的方向）：`φ = 1 − λ·clamp((u−1.5σ)/4.5σ, 0, 1)`。
  实测（强度10）：背景 76% 不变、线条 51%→61%（λ=1）、选择比 2.04→1.62、σ_out 9.14→10.34、**逆序 0**。
- R2「保护底色」（φ 随 u 上升）：线条仍 51%、背景 76%→91%、选择比 5.35，但 **a=1.5 逆序 144 / a=2 → 2089 / a=3 → 6253**，
  违反「压缩后偏离大小关系不能变」→ **否决**。用户的硬条件把方向唯一确定为 R1。
- 强度梯度仍严格线性（线条带每级多压 1.42 级）。

### 落地（四个文件）
`processBlockAverage` 增参 `useBlendIf`(默认 true)、`blendIfStrength`(默认 8)；φ 常量 a=1.5σ、b=6σ；λ = 级/10；
alpha 走不带 φ 的因子。UI 加「混合颜色带」开关（label-5）+「柔化」滑块（1-10，step 0.5，label-2）。
`helpTexts` 加 `blendIf` / `blendIfStrength`，`PanelStateManager` 类型同步。
历史记录名：开着对比减弱时 `applyProcessedPixels` 写「对比减弱」，否则「分块平均」。
脚本 `outputs/blockAverage-blendif-spec.cjs`（双方向 + 形态 + 单调性）、`outputs/blockAverage-blendif-ps-faithful.cjs`（忠实 PS 口径对拍）。

## 混合颜色带内置化（去 UI、参数写死）
用户决定：混合颜色带不再作为独立选项，**收进「对比减弱」内部，参数写死、不暴露 UI**。
- `processBlockAverage` 签名回到 6 参（删 `useBlendIf` / `blendIfStrength`）；φ 常量写死在函数内：
  `blendIfStart = 1.5τ`、`blendIfSpan = 4.5τ`，**λ 固定 1**（`phi = q>1 ? 0 : 1-q`）＝上一轮滑块的最大柔化档。
- 清理点共 14 处：AdjustmentPanel（状态 2 行 / 恢复 3 行 / 保存 2 行 / 依赖 2 行 / 处理器 11 行 / 调用 2 行 /
  滑块注册表 1 行 / switch 1 行 / UI 块 22 行）；helpTexts 删 `blendIf`+`blendIfStrength` 两键，
  并在 `contrastReduce`、`contrastReductionIntensity` 里补一句「内置柔化」；PanelStateManager 删 2 个类型键。

### ⚠️ 本轮踩到的两个工具坑（重要）
1. **本仓文件行尾不一致**：`src/adjustments/blockAverageProcessor.ts` 是 **CRLF**，`AdjustmentPanel.tsx` 是 LF。
   含换行的多行 `old_string` 在 CRLF 文件里**必然失配**（纯单行内的替换不受影响，所以签名那处能成功而注释块不能）。
2. **Edit「假成功」本轮高发**：12 次 Edit 只有 3 处真正落盘（复位块、注册表行、phi 块），其余静默丢弃且返回 success。
   可靠姿势：**node 脚本按行数组 splice + 写回前逐行断言 + 打印 OK/FAIL**（不要依赖长多行字符串匹配）；
   改完必须 grep 逐关键字复核计数。

## 按钮改名「扣白/扣黑」→「扣除纯白/扣除纯黑」+ 梯度修改负值写回范围修复
### 1) 按钮改名（4 文件，本轮 Edit 全部正常落盘）
- `AdjustmentPanel.tsx`：按钮文案改名并换宽度档 `action-button-2`(46) → **`action-button-4`**(72，=13×4+20)；
  `handleKnockout` 里的 `label`/`runCommand`/提醒与失败提示统一改为「扣除纯白/扣除纯黑」。
- `knockoutBatchProcessor.ts`：历史记录名（PS 历史面板可见）+ 全部注释同步改名。
- `helpTexts.ts`：注释与 `knockoutBlack` 文案里的「扣白流程」→「扣除纯白流程」。
- ⚠️ 未改：`README.md`、`docs/toolbox-guide.html`（还写着「扣白/扣黑」，等用户确认后同步；docs 带
  `data-page-node-id` 疑似外部编辑器导出，本地改可能被下次导出覆盖）。

### 2) 梯度修改（gradientRelaxProcessor.ts）负值 bug：a=0 像素被注入 alpha
**根因链**：进入时先把 RGB 预乘（`a===0` 的像素 RGB 被清零）→ 负值走 `blurAlpha` 分支，把邻居 alpha 的
邻域加权均值摊到**所有选区内像素**（含 a=0）→ 反预乘时这些像素 RGB=0、alpha>0 → **半透明黑边/暗色光环**
（radius = 2+2·|amount|，量级 5 时半径 12，视觉上就是一圈 20 多 px 的黑雾）。
正值天然无此问题：`mn` 恒等于自身 alpha=0 ⇒ `tn2=0 ⇒ v2=0`，a=0 像素保持 0（所以用户说「**仍然**只影响 alpha>0」）。
**改法（3 处）**
1. 通道循环里加统一不变量：`if (hasAlpha && a0 === 0) continue;`（`a0 = pixels[p+3]`），并复用 `a0` 给颜色通道权重。
2. 负值 alpha 分支不再重复读 `a0`。
3. 末尾反预乘 pass：`a0 === 0 || 选区内 == 0` 的像素**整像素还原原始字节**（原来整幅图都走预乘→反预乘往返，
   有 ±1 舍入漂移；选区内 a=0 的脏 RGB 也会被清零，现在不再动）。
**验证** `outputs/gradrelax-verify.cjs`（ts 源码 transpile 后跑合成图 64×64：方块 255 + 周围 a=0 脏 RGB(7,8,9)，
选区左 4 列外）：amount = -5/-10/0/+5 全部 PASS —— 选区外 4 字节零差异、选区内 a=0 像素 4 字节零差异、
负值时 alpha>0 像素确实被改动。抽样（-5，方块边缘 x=20）：255→191、RGB 提亮为保持黑底观感。
⚠️ 副作用/遗留：a=0 被冻结后「放缓」只能**向内**摊（硬边 255→约 191 且随 radius 向内延伸），
不再向外扩 alpha；若用户要「不缩形」的柔化，需改成把 a=0 排除出 blurAlpha 权重（未做，等反馈）。
`outputs/gradrelax-verify.cjs` 已随本轮验证写盘（outputs/ 已 gitignore）。

## alpha 对齐：斑驳/条纹根因 + v5 修复 + 众对齐（14:20–14:45）

**用户素材**：`C:\Users\Administrator\Desktop\实验.png`（124×147，4569 个 alpha>0，全文档选区），
两张垫纯白截图分别是 v4 的下/上对齐结果。**先复现**：把 PNG 解成裸 RGBA、transpile TS 源跑真函数、
回合成垫纯白 PNG —— 复现图与用户截图逐点一致，确认拿到同一个 bug。

**根因（有数据）**：v4 逐像素独立估计参照 → 相邻像素选中**不同层级**。
区块 x[44,60) y[55,67) 实测：源 143/184/201/182/157… → 结果 67/67/65/65/65…，
但同一带里 158/160/162/143 等孤立点原样残留 → 棋盘式斑驳；沿笔触方向即条纹。
两个机制：①`bestRef` 取所有尺度候选的**最小值**，k112 环带整体跑出笔画、采到邻近浅色层的 73
（"取最低"→被污染）；②中尺度 `midFlat` 保护被大尺度 `midNonFlat` 反制 → 保护失效。
覆盖率也只有 1605/4569（35%）= "对齐不彻底"。
度量：一阶差分粗糙度 12.22/8.24（源）→ v4 下 10.56/8.89（纵向反而更差）、v4 上 12.44/9.79（**恶化 19%**）；
3×3 中位数离群点 284（6.2%）。

**v5 改动**（`src/adjustments/alphaAlignProcessor.ts`）：
1. **参照场空间一致化**（R=6，support=3）：下对齐取窗口内计数达标的最低参照，上对齐取下中位数；
   并对没拿到参照的像素补判（门槛 `BRIGHT_GAP/2 = 30`，可越过 `flatAll` 保护，但仍要求 `a ≥ MIN_ALPHA`）。
2. 平坦判据改**绝对数**；上对齐加"本层邻域"护栏（`bandMax > a + BRIGHT_GAP` 跳过该尺度）。
3. 删除 `withBg`（保底下对齐）及其 `BRIGHT_*_BG`/`BRIGHT_DELTA` 常量与全部分支。
4. 新增导出 `processAlphaModeAlign`（众对齐）；把选区羽化 support 抽成共享函数 `buildSelectionSupport`。

**结果**：下对齐 7.23/5.16、离群点 171（-40%）；上对齐 12.01/9.09（不再恶化，大黑斑消失）；
众对齐 0.000/0.000、不同值数 2、4422/4569 像素被改（全部 = 众数 70）。

**UI**：四按钮子分区（下/上/众/线条加黑，`.action-button-quad` 92px ×2×2）从「边缘处理」末尾
**整体挪到「快捷操作」最下方**（扣除纯白/扣除纯黑之后，前面补一条 divider）；
"保底下对齐" → "alpha众对齐"（`helpTexts.adjustment.alphaBgDown` → `alphaMode`，用法也跟着改了）；
`handleAlphaAlign(withBg, direction)` → `handleAlphaAlign(direction)`，新增 `handleAlphaModeAlign`。
tsc 仅剩历史报错、webpack 生产构建通过（dist/bundle.js 已更新，无"保底下对齐"残留）。

**工具链（可复用，outputs/ 已 gitignore）**：`aa_img.py`(dump/comp) + `aa_run.cjs`(transpile 跑真函数) +
`aa_diag.py`(离群点定位+区块对照表) + `aa_compare.py`(中文标签的多面板对比图)。
经验已回灌进技能 `.workbuddy/skills/algo-param-bench/SKILL.md`（含"Edit 报成功但没落盘"的复核要求）。

## helpTexts.ts 文案修订：第一版整体被回退，第二版 37/117 通过（15:00–15:40）

### ⚠️ 第一次尝试的失败原因（用户原话："像哄孩子一样介绍"）
1. **读者画像搞错**：受众是**精通 PS 的画师**——羽化、不透明度、通道、蒙版、中间值、混合模式、alpha
   **不需要解释**，展开讲反而啰嗦且显得外行；他们只对**算法与实现原理**一窍不通。
   判据：只有「PS 范围之外的词」（邻域、连通块、直方图、归一化、颜色传播源、高频/低频、事件驱动、
   全局键盘钩子、通知里不带工具名）才改写或删除，PS 原生术语**原样保留**。
2. **腔调变教程**：写成「一句话：……」「解决什么问题：……」「你可以……」「不用自己试」，
   通篇第二人称 + 设问句 → 出戏。要的是**人给人写的说明文**。
   补主语 = 补「插件 / 该值 / 选区」这类**语法主语**（仅在易歧义处补），**不是**加「你」。
   大白话 = 不用生僻词与画师不熟的词 ≠ 口语化。
3. **改动面过大**：117/117 全改 → 用户无法逐条比对，只能整体回退。
   **正解是手术式修订，只动真有问题的条目，合格句子逐字保留** → 第二版改 37 条、留 80 条。

### 第二版落地的修改点（保持原文骨架与语气）
- **去自造/算法词**：imageAPI、对选区灰度通道用高斯模糊 → 等效于 PS 选区羽化；
  "降低灰度通道高频信息" → 等同于【选择并遮住】的平滑；"邻域大小" → 参考范围；
  "高频细节/低频形状" → 纹理细节 / 大的形体与明暗结构；"连通块" → 不相连的选区；
  "颜色传播源/几何补全" → 颜色来源 / 直接消除；"直方图众数" → 出现次数最多的 Alpha（保留"众数"括注）；
  "归一化映射/形状质心" 整句删除；"非对称平滑" → 沿线条方向平滑；"事件驱动+兜底轮询" → 直接删。
- **补用途（用户抱怨"不理解这功能干嘛"）**：
  · `powerfulMode`：改为「半径由插件自动推算，适用于不确定半径该设多少、或笔触粗细不一的情况」+
    「处理方式由高斯模糊改为中间值，自动半径按最粗笔触推算，15px 约对应 18」。
  · `knockoutWhite/Black`：删掉整段"复刻手动验证方案：Ctrl+点击 RGB 通道→Delete→复制 N 份合并"，
    改为**用途（白底/黑底半透明内容抠回透明底）+ 检验标准（放回原底色视觉一致）+ 图层限制**。
- **删开发计划**：selectionExpand 第 2 条、clearMode 的"未来考虑相对计算模式"、patternDetail 的
  "接口未开放…当开放后会跟进"、gradientDetail 的"由于浏览器的问题…未来可能会补充"。
- **消歧义补主语**：mainButton 的"功能开启后" → "主开关开启后"；新增"与 PS 相反"点明渐变不透明度色标反向。
- **语义修正（读源码核实）**：`blockGradient` 原文"每个连通块取形状质心，沿渐变方向投影后做归一化映射"
  → 实为**每个不相连选区只填一个纯色**，颜色按其画面位置取自同一渐变（`blockGradientProcessor.ts:280-305`），
  改写为"多个选区连起来看，相当于分布在同一层渐变上"。

### 验证姿势（沿用本仓"Edit 假成功"教训）
Write 全量重写后必做三件事：①python 正则抽 key 与 `git show HEAD:` 比对集合/顺序；
②node `ts.transpileModule` + `new Function` 实跑导出对象（能一并验证 `recordHint` 里的 `\\'` `\\` 转义）；
③术语黑名单 grep（imageAPI/邻域/连通块/归一化…）确认清零。
注：`git checkout .` 会连 `.workbuddy/memory/` 一起复原，回退后需重新补记。

## 还原「铅笔去锯齿」→ 改名「消除锯齿」并推广到所有色块轮廓 + 修湿边（15:50–16:20）

提交 `1ac1e6d` 删掉了 `pencilAASmoothProcessor.ts`（751 行）。本轮从**父提交**取回原文件
（`git show 1ac1e6d^:src/adjustments/pencilAASmoothProcessor.ts` —— 注意直接用该 commit 的 `:path` 取不到，
因为它在这个提交里正是被删的），改名为 `src/adjustments/aliasSmoothProcessor.ts` / `processAliasSmooth` /
`defaultAliasSmoothParams`，放回**边缘处理**分区（order 1，夹在 边缘平滑 0 与 线条加黑 2 之间）。

### 湿边根因（数值已证）
原版把「覆盖率 → 不透明度」写成一张**按不透明笔触标定的绝对表**（0~255，F_TABLE_Y），于是：
- 半透明形状的**轮廓外围**被推到接近 127（旧代码写死 `aRecon = Math.min(aRecon, 127)`）。
  本体 140 时 127 已接近本体；本体低于 127 时就是"边比本体更实"。
- 二值化阈值写死 128：本体 <128 的半透明色块**根本进不了 mask**（什么都不发生）；
- `细线保护` 关掉时 `domainMax` 钳位也被跳过 → 内侧像素直接吃 F 表，**冲到 253**。
  合成图实测（本体 140 硬边圆）：旧版轮廓剖面 `104 211 255 255`、**624 个像素高于本体**；
  新版 `57 116 140 140`、0 个越界。

### 改法（四条不变量，缺一不可）
1. **阈值自适应**：`estimateBodyLevel` = 选区内 alpha>0 的**粗直方图众数**（8 宽一档），
   `thr = clamp(round(bodyLevel/2), 2, 128)`。不透明形状 → 248 → thr=124（与旧版 128 等价）；
   半透明色块才可能进 mask。
   ⚠️ **取值必须只依赖「哪一档胜出」**（用档的下边界 `bestBin<<3`），不能用档内均值：
   第一次处理新生成的过渡带像素会改直方图，均值漂移→阈值变→二次运行出现零散改动。
2. **本体不透明度场** `bodyAlpha`：种子 = 形状内「距轮廓 ≥2px」的像素（`distOut2 >= 4`），
   宽 ≤5px 的窄条整段算种子（`seedThinFlag`，否则窄条取不到本体水平）；EDT 传播最近种子的 alpha。
   `aRecon = aBody × profile(cov)`（profile 已归一化成 0~1），**硬性封顶 `aRecon ≤ aBody`**。
   同一张图上不同透明度的色块各按自己的水平处理。
3. **两侧单调性**：内侧 `aRecon ∈ [thr, 原值]`（**只削不抬**）、外侧 `aRecon ∈ (原值, thr-1]`（**只补不削**）。
   内侧只削不抬这条是必需的：软边半透明笔触（原剖面 46/93/140）在只有"≤本体"封顶时仍会被
   重新硬成 46/116/140；加"只削不抬"后 106 保持不动。
4. 锚定（mask 内 ≥thr、mask 外 ≤thr-1）保留 → 输出 mask 与输入完全一致 = 严格幂等。

### ⚠️ 本轮挖到的两个坑
1. **本体种子自身不能改**：凹角/窄颈处 box 窗口外溢，`cov` 掉到 0.97 以下 → 那些"距轮廓 ≥2px"的像素
   被当成轮廓像素降 1 级 → 它们同时是 bodyAlpha 种子 → 本体水平 255→254 → 二次运行整体下漂 1 级。
   修法：循环里显式 `if (distOut2[ri] >= 4) continue;`（那一带本来 cov≈1，等于把 cov≥0.97 的跳过提前）。
2. **删掉的两段是死代码/被取代**：`bgAlpha`（稳定背景 EDT）与"色块保护"判据 `bgAlpha > 127` 恒不成立 ——
   背景种子要求 `mask==0`，即 alpha < thr，所以 bgAlpha 永远 < thr；"背景不被侵蚀"已由不变量 3 的
   "外侧只补不削"从根上保证 → 整段（含一遍 EDT 与抬底 `aRecon = max(aRecon, bgA)`）删除。

### 验证（outputs/，已 gitignore）
`alias_smooth_test.cjs`（4 用例 × 6 断言：≤本体 / 外侧只补 / 内侧只抬 / mask 不变 / 幂等 / 剖面）、
`alias_compare_old_new.cjs`（旧版对拍，湿边量化）、`alias_trace.cjs`（patch 副本打印中间量定位漂移）。
用例含：半透明硬边圆 100、不透明硬边圆 255、1:20 缓坡厚带（长台阶插值分支）、柔和渐变边。
**四个用例全部：无越界、mask 不变、二次运行残留 0 像素。**

### 接线（`AdjustmentPanel.tsx` 共 9 处 + 2 文件）
import、SubFeature 联合类型、`defaultSubFeatures` 增 `{aliasSmooth, 边缘处理, order 1}`、状态 2 行、
加载 2 行、持久化 values 2 行 + deps 2 行、复位 2 行、handler 3 个、`SLIDER_DRAG_CONFIGS.aliasSoftWidth`
+ switch case、`handleAliasSmooth`（沿用现行写法：`runCommand` + `doc.suspendHistory` 一条历史 +
`getSelectionBounds(false)` 探测后 `selectAllDocument()` 并入同一历史 + `runWithTemporaryUnlock` +
`applyProcessedPixels(..., {skipHistorySuspend:true})`）、UI（`action-button-4` + `label-4` 细线保护开关
+ `label-drag label-4` 柔化宽度滑块）。`helpTexts` 增 `aliasSmooth`/`aliasSoftWidth`/`aliasThinProtect`
（117→120 条），`PanelStateManager` 类型增 2 键。
未改：`README.md`、`docs/toolbox-guide.html`、`dist/docs/*`（边缘处理清单仍只有边缘平滑/alpha 对齐/线条加黑，
待用户确认后再同步）。前端改完需 **UDT Reload**。

## 消除锯齿：去掉「细线保护」+ 细线专项几何重建（下午）
- 删「细线保护」全线：参数/handler/UI 开关/helpTexts 键/PanelStateManager 字段，及处理器里的兜底高斯块。
- 细线为什么没效果：细线在 blur 窗口里占比 <15%，cov 恒 <0.22 直接 continue；F 表按实心区标定，硬走覆盖率只会把整线压到阈值附近（变淡不是消锯齿）；细线内部又没有 distOut2≥4 的像素可作本体锚点。
- 新增 processThinBands：主扫描轴切游程 → 按重叠串链 → 平台中心插值还原亚像素边界 → 刚性带宽（众数厚度）+上下边界均值定心 → 墨量守恒重分布 ink·cov/Σcov。抖动 Δ² 1.00→0.06~0.34，二次残留 0。
- 幂等铁律①：ink 跨距取几何区间 [⌊eT⌋,⌈eB⌉-1]，不能取 mask 游程（肩部落到阈值以下会掉出 mask，墨量逐轮流失）。
- 幂等铁律②（幂等闸门）：带窗口内一旦出现 0<alpha<窗口最大值 的过渡像素 → 判定已平滑，只认领不改写。亚像素信息在平滑后不可反推（1px@255 与 2px@127 像素值完全相同）→ 细线只能一次性处理。
- 主路径避让：thinScope（认领→主路径 continue）+ thinDone（两轴向去重）；认领 mask 游程 ±2px，否则 3~4px 线外侧会被主路径补出 ~107 虚边。
- thinFlag 阈值 2.25→4（线宽 ≤4px 归细线专项），seedThinFlag 合并删除。
- 验证台 outputs/alias_thin_test.cjs：7 案例（1/2/3/4px 斜坡、45°、半透明 2px、厚色块对照）全部 掩膜翻转 0 / 二次残留 0 / 内只降外只升 / 输出峰值=本体。
- 测试台坑：光栅器用 <= 且中心恰在整数上会出 3 行退化列；把阈值写死 128 会把半透明案例（真阈值 68）误报成掩膜翻转。

## 消除锯齿：细线"没有任何现象"的根因修复（傍晚）
用户在真实画布上画两根细线（截图 144×978，1px 与 8px 宽）→ 点「消除锯齿」毫无反应。
- **根因①（决定性）**：estimateBodyLevel 直方图**按像素计数**投票，而细线的抗锯齿毛刺像素比本体还多
  （1px 线每行 ~2 毛刺像素 vs 1 本体像素，实测 828 vs 414）→ 众数落到毛刺那一档 → thr 从 124 塌到 4，
  mask 把整圈毛刺吞掉；再叠加旧闸门"窗口内出现任何中间 alpha 就跳过" → 判定"已平滑"→ 整条线什么都不做。
  修法：hist[a >> 3] += a（按墨量投票）。半透明/细线/毛刺场景全部恢复正常，主路径 4 案例不受影响。
- **根因②**：细线幂等闸门判据错误（"有中间值"≠"已平滑"）→ 改成"边缘是否已成亚像素过渡"：
  本行游程内最大 alpha 为基准，窗口内出现 0.25~0.75 基准的像素的行 > 半数 → 判已平滑，只认领不改写。
  基准必须**逐行**取（不能用全链最大）：笔触带压感渐变，用全链最大会把渐细段整段误判成已抗锯齿而跳过整条。
- **根因③（端部）**：平台中心插值把"被链边界截断的首/尾平台"的观测中心当锚点 → 端部 ~15 行亚像素位置偏最多
  半个平台长（1:40 斜坡实测 0.38px，占 7% 行）。修法：按"典型平台长度（内部平台中位数）"重建首/尾平台中心。
  修后线心偏差：1:40 → 0.011、3px 1:25 → 0.012、1px+毛刺 → 0.092（原来 0.35~0.49）。
- **根因④（墨量蒸发）**：内圈被 aRecon ≤ a0 卡住的墨直接丢掉 → 每次点击峰值降 2~3 级（爬行）。
  修法：把截断丢掉的墨转投到"外侧还有余额"的肩部像素（cap = thr-1）。修后连点 6 次峰值恒 255、总墨量 -0.02%~-1.8%，
  且线心误差随点击**单调收敛**（0.092→0.086→0.083），不会越点越软。
- 另修：写出循环不再外推写链外一行（原来会在空像素上凭空造出一条虚边）。
- 验证台：outputs/alias_battery.cjs（13 案例：近垂直/近水平 × 线宽 1~4 × 本体 90~255 × 纯硬/5%毛刺/50%毛刺）、
  outputs/alias_user_scenario.cjs（复刻用户两根线，1:1 与细线解读两种尺度）、outputs/analyze_clip.cjs
  （**直接解码用户截图 PNG、灰度反推 alpha 剖面**判断"硬阶跃 vs 已抗锯齿"—— 复现真实工况的好办法）。
- 光栅器老坑复现：中心恰落在整数/半整数网格上会出"空行"（1:100 斜率下每 100 行断一次链），
  会把"链被切断成单平台段"误读成算法缺陷 —— 造测试线时必须给相位加偏移。

## 「仅主线条」边缘平滑：窗口化重构（本轮）
- 诉求：line 模式低配机极卡，要求 1000×1000 选区也流畅。
- 根因：原实现整图处理 —— 4000² 文档 = 16M 像素、十多个 Float64Array（每个 ~128MB）、EDT 逐列 stride 访问 cache miss、
  多源 BFS 每像素边界判断 + 取模、中位数全量排序。
- 改法（src/adjustments/lineSmoothProcessor.ts 重写）：①窗口化 = 选区包围盒 + halo（halo = max(8, 2·kr+8)，kr = ceil(3σ) ≤ 10），
  1000² 选区 16M → 1.1M 像素；②buildSignedDistance 返回 Float32Array（INF2 = max(1e6, 4(w²+h²))），edtSquaredInto 改 64 列分块 +
  32 行 tile 转置访问；③高斯 gaussianBlur(src,w,h,sigma,tmp,out) 内区免边界判断、垂直方向逐行累加；
  ④多源 BFS 改哨兵圈（P=rw+2）+ 限深（BFS_MAX_DEPTH = ceil(kr·1.5)+2），消除边界判断/取模/整图遍历；
  ⑤中值归一改三级直方图细化 floatMedianRefined，不排序不收集样本。
- src/adjustments/smartEdgeSmoothProcessor.ts：把 `if (mode === 'line')` 分派提前到二值化整图掩码/拷输出缓冲/扫包围盒之前，少跑 3 遍全图。
- 结果（outputs/lineSmooth_perf.cjs + _ls_c1_pair.cjs，预热多次取最优）：4000² 文档 / 1000×1000 选区 17119ms → 478ms（**35.8×**）；
  2000² 文档同选区 2966 → 465ms（6.4×）；1000² 全图选区 724 → 369ms（2.0×）；5000×4000 文档 + 1500×1200 选区 845ms。
- 等价性：全图选区逐字节一致；差异只来自「跨窗口边界的连通域分量覆盖率统计」，方向恒为「新保留、旧清除」（旧>新 恒为 0，保守不误删），
  1000² 选区 0.003% / 100×100 贴角 3.0% / 十字带 0.32%。halo=96 只对部分场景有效且 +28% 耗时 → 放弃，维持现状（已写进 MEMORY.md）。
- 顺带修台架标签 bug：diffStats 的 up/down 是「旧−新」的符号，原打印把 新>旧 与 旧>新 写反了 → 已对调（否则会误读成"新版会多删像素"）。
- 踩坑：nearSrc 若存局部索引，写回取色必须换算回全图坐标（否则 RGB 全 0）；中位数不能取整档（medBlur 差 0.019 → bodyScale 差 1e-4 →
  系统性压暗 1 级、0.16% 像素 ±1）。
- 备份 outputs/lineSmoothProcessor.orig.ts.bak（改 .bak 后缀避免被 tsc 收录）；webpack production 构建通过（仅既有体积警告）。
