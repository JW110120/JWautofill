# 2026-09-10

## 新增「专注模式」（Focus Mode）
- 成立条件（推导值，不存 state）：APP 父面板 `autoOffOnOtherTool` && `switchToLassoOnEnable` 同时勾选。
- 新增 `src/utils/FocusModeBus.ts`：共享文件 `settings/focus-mode.json`（readFocusMode / setFocusMode / subscribeFocusMode，400ms 轮询 + 同上下文即时通知）。跨面板语义同 MainToggleBus。
- `MainToggleBus.doToggle`：专注模式下热键「只开不关」（已开则原样返回，绝不成关）；关闭只能靠切工具触发 autoTurnOffMain。
- 视觉：主开关圆点 → `FocusStarIcon`（Icons.tsx，13×13，与 .indicator-lg 同尺寸），颜色复用 .indicator-disabled 灰 / .indicator-ok 绿（新增 .indicator-icon-lg / .indicator-icon-ok，common.css）。
- 工具箱笔刷热键分区：专注模式下置顶记录文案「选区填充开关」→「选区填充」（title 走 entryPinnedFocus）。
- app.tsx：`isFocusMode()` / `syncFocusMode()`（值变才写文件），componentDidUpdate 与 mount 加载完成后各调一次。
- 未改动：主开关手动点击仍可自由开关；底部热键 toast 仍写「选区填充开关」（如需一致可再改）。

## 修：主开关圆点切换时左移 ~3px
- 根因：`.main-button-content` 是 width:100% + justify-content:center 的居中 flex 行，两态字号不同（开启 `.main-button-text`=16px 粗体 / 关闭 `.label-disabled` 仅改色、继承 14px）→ 组宽变化 ΔT，居中基准移动 ΔT/2，左侧圆点被挤动；文字中心恒在圆点右侧固定偏移处，所以看起来不动。
- 修法：两态共用定宽居中槽 `.main-button-label`（width:64px = 4 字×16px；text-align:center; flex:none; white-space:nowrap）。开启态像素不变，关闭态圆点左移 4px 后即与开启态同位置，切换不再跳动。

## 修：面板高 ≈823px 时滚动条中间态裁掉右侧原生控件
- 现象：APP 主面板在 ~823px 高度出现「有滑块条 / 无滑块条的中间态」，描边模式的 sp-switch 与填充模式齿轮的右半截被裁；工具箱无此问题。
- 根因：PS/UXP 滚动条由宿主原生绘制，**只按需占位**——内容溢出才挤掉 10px，不溢出就还给内容。common.css 里为它写的 `scrollbar-gutter: stable` 与 `::-webkit-scrollbar` **在 UXP 下均不生效**（Adobe 官方论坛：UXP 不支持自定义滚动条），故内容盒在 230↔220 之间翻转。高度 823 时内容高正好压在阈值上：滚动条一出现，CSS 重排到 220px，而 UXP 原生控件（sp-switch / sp-radio 的 slot 内容）来不及重排、仍按 230 画在 x=240（旧内容盒右缘），恰落在滚动条槽位上被后绘制的滚动条压住。工具箱无症状＝其内容永远溢出、滚动条从不消失（内容盒恒 220）。
- 修法：`app.css` 加 `#app .app-root > .panel { overflow-y: scroll; }`，把 APP 主滚动容器钉在「滚动条常驻」状态，内容盒永久 220px，不再翻转。
- ⚠️ 只限 `#app .app-root > .panel`：4 个子面板是独立 `.panel`，其 228px 内宽链（pattern.css）按「无滚动条」标定，不能一起改；工具箱 `.panel` 上有 position:fixed 锁屏遮罩依赖现状，也不动。
- ⚠️ 无效尝试（勿重犯）：别想用 `padding-right` 预留槽位——Chromium 的 scrollport = padding box − scrollbar，加 padding 只会让内容盒再窄 10px（220→210），位移照旧。

## 新增「紧凑模式」（右上角菜单，默认关，随面板持久化）
- `types/state.ts`：`compactMode: boolean`（initialState=false）。`PanelStateManager.AppPanelState` 加同名字段。
- `MenuManager`：新增 `onToggleCompactMode` 回调 + `toggleCompactMode` case + 菜单项 `{id:"toggleCompactMode", label:"紧凑模式"}`，位置紧接 `resetAppParameters` 之后、`spacerApp1` 之前。
- `app.tsx`：`toggleCompactMode()`（取反）+ `syncCompactModeClass()`（幂等挂/摘 `body.compact-mode`）；constructor 绑定；componentDidUpdate 里在 compactMode 变化时同步；mount 载入面板状态后调一次恢复持久化值；加进 watchedKeys 与 load/save 的 appPanel 字段；`onResetParameters` 里保留当前值（界面开关不属于「参数」）。
- 底部 info 条外层 `.panel-section` 追加 `.panel-footer` 类（整块隐藏，避免只藏版权后留下该分区 15px 下外边距）。
- `app.css`：`body.compact-mode #app .main-title, body.compact-mode #app .panel-footer, body.compact-mode #app .divider { display: none }`，仅作用 #app，工具箱不受影响。
- 已验证：webpack production 编译通过（仅 3 条原有体积警告），dist 中确认 `overflow-y: scroll`、`body.compact-mode`、`紧凑模式` 均已落地；`body.secondary-panel-open` 的 `overflow-y:hidden` 权重 (1,3,1) 仍高于新规则 (1,2,1)，次级面板打开时照常锁滚动。

## 修正：滚动条问题（推翻上面的 overflow-y: scroll 方案）
- 用户反馈：内容盒恒 220 破坏了原设计（无滚动条时设计要 230）。要求恢复原生 230/220 两档。
- 拿用户截图（250×823）逐像素量：分隔线 x=10..229 → 内容盒 220px；右侧 14 列（236..249）**没有任何滚动条像素**；填充模式齿轮右缘被竖直切在 x≈230、只露 9px（图标 18px）；清除模式 sp-switch 只露 13px（左半）。
- 新根因：`scrollbar-gutter: stable` 在 UXP 的 CSS 侧**确实生效**（预留 10px → 内容盒 220、无滚动条绘制＝用户说的「中间态」），但 **PS 宿主给原生控件排版时不扣这份槽位** → 控件按内容盒 230 摆位、右缘落到 240 探出 10px，被面板右缘裁掉右半截。工具箱（行在 `.border-panel-section` 里，padding 10px）多出的 10px 正好落进 padding，所以看不出。
- 修法：**删掉 `.panel` 的 `scrollbar-gutter: stable`**（common.css），app.css 里删掉 `overflow-y: scroll` 兜底。槽位回到按需占位：无滚动条 230 / 有滚动条 220，CSS 与宿主始终对齐。
- 教训：`::-webkit-scrollbar` 不生效 ≠ `scrollbar-gutter` 不生效，当初把两者一起当死代码是错的。

## 紧凑模式：补「清除模式→填充模式」间距
- 量得行节奏：描边/清除行中心间距 53px = 行盒 32px + 折叠后 10 + divider 1 + 10 → 行盒 32px（sp-switch 32px 高），分区首行标签盒 22px。
- 隐藏 divider 后：行→行 外边距折叠仍 10px；行→分区只有行的 10px，且 (32−22)/2 = 5px 的盒高差让视觉间距更紧。
- 修法（app.css）：`body.compact-mode #app .app-root > .panel > .panel-section .divider + .panel-section { margin-top: 15px }`（10 基线 + 5 补偿，折叠后取 max(10,15)=15）；选择器刻意收窄在主面板滚动区内——子面板里 divider 后的分区首个子元素是自带 10px 上边距的 `.row-between`，套用会过量。


## 三项改动（用户当轮提出）
### 1. 专注模式图标换成五角星
- 用户指定 Spectrum「S Star 18 N」：`src/styles/Icons.tsx` 的 `FocusStarIcon` 换 path（`M9.2385.2965,...`）。
- 尺寸仍 13×13、viewBox 0 0 18 18、fill 走 `.icon-fill`（沿用圆点那套启用绿/禁用灰双色），保持主开关不位移。

### 2. number-input 圆角 3px
- `common.css` 的 `.num-input-row` 加 `border-radius: 3px`（全插件唯一数字输入框容器，内部 input 靠 overflow:hidden 裁角）。
- 确认过：全仓 `type="number"` 都包在 `.num-input-row` 里，无第二处定义。

### 3. 紧凑模式拆成 5 个独立作用域
- 需求：APP 父面板与 4 个子面板互不干扰；菜单文案按当前面板显示「XX面板紧凑模式——已开启/已关闭」。
- `types/state.ts`：`compactMode: boolean` → `compactModes: CompactModes`（`CompactScope='app'|'color'|'pattern'|'gradient'|'stroke'`，`initialCompactModes` 全 false）。
- `app.tsx`：`COMPACT_CLASS`/`COMPACT_NAME` 两张静态表 + `compactScopeOf(state)`（子面板打开优先 stroke→pattern→gradient→color，否则 app）；`toggleCompactMode()` 只翻当前作用域；`syncCompactModeClasses()` 挂/摘 5 个 `body.compact-*`；`syncCompactMenuLabel()` 写文案。componentDidUpdate：compactModes 变→类+文案；仅当前面板变→只写文案。
- `MenuManager.setCompactModeLabel()`（新增）：getItem→updateItem→改数组项三级降级，与 setLicenseLogoutEnabled 同款。菜单项初始 label 改「选区填充面板紧凑模式——已关闭」。
- 4 个子面板根节点加作用域钩子 `.subpanel-color/.subpanel-pattern/.subpanel-gradient/.subpanel-stroke`；app.css 用 `body.compact-* #app .subpanel-* .divider` 只藏本面板分隔线。父面板 divider 选择器收在 `#app .app-root > .panel > .panel-section` 内，不再越界到子面板（旧 `body.compact-mode #app .divider` 一条通吃正是要拆的耦合）。
- 顺手修：载入面板状态时原先**没有**把 compactMode 合并回 state（只在 initialize 里传了默认值）→ 紧凑模式根本没被恢复过；现按 5 个作用域逐项 `??` 回落后再 setState。
- 校验：webpack production 通过（3 条既有体积警告）；bundle.js 中新星形 path 1 处、旧 path 0 处、「面板紧凑模式」1 处、5 个 compact-* 与 4 个 subpanel-* 钩子齐全；dist/common.css 与 src 一致且含 `border-radius: 3px`。

## 本轮两项收尾改动（用户精炼需求）
### 1. 紧凑模式菜单文案精简
- 旧：`选区填充面板紧凑模式——已关闭`（冗长）。新：`紧凑模式：{面板名}·开/关`，如「紧凑模式：选区填充·关」「紧凑模式：图案·开」。
- app.tsx `syncCompactMenuLabel()` 模板改为 `紧凑模式：${COMPACT_NAME[scope]}·${on?'开':'关'}`；MenuManager 初始 label 改「紧凑模式：选区填充·关」；app.css 注释同步。

### 2. 紧凑模式下填充模式三行→三列 radio（仿描边子面板「位置」）
- app.tsx：`compactApp = !!compactModes?.app`；填充模式分区在紧凑态渲染 `<div className="radio-trio"><sp-radio-group>` 三列、不渲染 IconButton 齿轮，3 个标签 `<span className="label-2" onClick={打开对应子面板}>` 点文字即选中并开子面板；非紧凑态保持原三行 + 齿轮不变。
- `.radio-trio`（含 `sp-radio:not(:last-child){margin-right:25px}` 与整组 `translateX(10px)`）从 stroke.css 的 `.position-radio-group` 收口为 common.css 通用类；StrokeSetting.tsx 根节点改 `panel-section radio-trio`。两处必须一致。
- helpTexts.selectionFill 新增 fgCompact/patternCompact/gradientCompact 三句紧凑态提示。
- ⚠️ 构建注意：dist/common.css 由 CopyWebpackPlugin 复制 src/common.css 而来；app.css 被 index.tsx import 后由 css-loader 内联进 bundle.js（运行时注入），故 app.css 内容不在 dist/common.css、而在 bundle.js。校验须查 bundle.js 而非 common.css。产物时间戳比构建时间旧属正常（copy 插件按内容去重）。
