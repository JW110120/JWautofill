# 2026-09-07

- 排查「FixKeyboard.exe 未同步到远端」：exe 早已入库且远端 main=c762a3e 就包含它；真相是本地 .git/refs/remotes/origin/ 目录缺失，fetch 假成功、origin/main 停在旧值 a1ab315，status 恒显 ahead 83。已 mkdir+update-ref 修复，并推送当日新提交（远端=1f7ab83）。公司侧重新 git pull 即可拿到 exe。
- 新增 push 方法：`git -c credential.helper=wincred push`（portable git 的 helper-selector 非交互不可用；凭据管理器存有 JW110120）。
- 用户实报键盘冻结第三形态：按键不停重复、重插才恢复。定性=设备层变体（key-up 报文丢失、键卡按下态），应急先按同一键补 make+break；与②同修法。已写入 fix-keyboard.ps1 注释与结尾提示（native+dist 同步，BOM 保留）、提交推送，并更新技能 windows-keyboard-device-reset。
- MEMORY.md 超限截断，已重构压缩至 2996 字符（7800+→2996），细节下沉到技能。
- UI 收口三连：① app/纯色(ColorSettingsPanel)/图案(PatternPicker) 共 5 处单行滑块(.slider-block 两行式)全部改行内滑块(.row-between+label-drag+slider-track+row-start)；common.css 删 .slider-block/.slider-input 及禁用态（通用 .row-between.disabled 已覆盖）、app.css 删 .slider-container .slider-block 并把水平 padding 并入容器。② manifest.json 绘画工具箱 4 处 width 300→250；笔刷热键 notify-bar 文案精简为「服务已就绪/服务处理中…/服务未启动」与「停止服务/启动服务」（完整说明仍在 title）。③ 渐变面板颜色行 # 加 .num-unit-hash(margin-right:8px，gradient.css)。yarn build 通过。
- 间距四连修：① gradient.css 加 .border-panel-section .subpanel-title-2 负 margin(-10px 通栏顶格,padding 0)；② common.css 新增 .row-between .row-between 嵌套边距清零(+相邻行 10px)——根因是 flex 列内 margin 不折叠致 checkbox 行间距 20px，全库扫描确认嵌套仅 app/gradient 两处 checkbox 网格；③ pattern.css .border-panel-section>.panel-section 下边距 15→10(radio 行上下对称)；④ common.css 两/三列 radio 标签 margin-top:4px 对齐圆点(sp-radio-group:not(.radio-group-vertical)，覆盖 >span 与 >.row-end>span)。顺手修 GradientPicker/PatternPicker 两处 column default→column-default 笔误。build 通过。
- 间距/居中四连修（第二轮）：① checkbox 大间隙第二轮根因=外层行 10px 外边距与相邻 divider 10px 叠加成 20px + 原生 checkbox 默认 margin；修法=common.css 新增 .checkbox-grid{margin:0 auto} 挂到 app/图案/渐变三处 checkbox 网格外层行 + .checkbox-input{margin:0}（行左 4px 间距由更高特异性规则保留）。② divider-vertical 居中：hotkey-entry-combo 由 flex:0 0 110px 改 flex:1 1 0%+min-width:0，与名称列各占一半，分隔线严格居中（300→250 后 110px 定宽必偏右）。③ 蒙版同步 + 号：新 .mask-sync-add-row（min-height:52、flex 居中、margin-bottom:-10px 吃掉容器 A 底 padding），上下各 10px 对称；TSX 由 row-center 换类。④ 纯色 .color-calc-radio 与图案 .pattern-fill-radio 两列 radio：删 translateX(10px) 老补偿，改 space-between+sp-radio flex:0 0 auto+去 70px 间隔，两选项各贴容器 10px。build 通过。
- radio space-between 回归修正：实测 UXP 的 sp-radio-group 不支持 justify-content:space-between（影子布局自管排列，flex:0 0 auto 后两选项挤成一对）。改回通用 flex:1 均分机制，仅把 .pattern-fill-radio/.color-calc-radio 的 :not(:last-child) margin-right 提到 100px（原 70px，用户嫌中缝小）：左选项圆点贴容器左、右选项文字贴容器右（各约 10px 由分区 padding 提供）。新经验：sp-radio/sp-radio-group 上只信 flex:1+margin 定位，不信 justify-content。build 通过。
- 两列 radio 贴边第三轮（定稿方案）：调中缝 margin 数值无解——右选项内容在自身半宽盒内左对齐，文字右缘离容器距离=盒宽-文字宽，永远随文字宽度漂移（80/100px 均不合要求）。改为宽度无关方案：common.css 新增 .radio-pair-230(纯色,可用宽230)/.radio-pair-210(图案,可用宽210) 两类——组挂 position:relative，首选项 flex:0 0 auto 留文档流（圆点=分区 padding 10px），末选项 position:absolute;top:0;right:0（盒收缩为内容宽，文字右缘=容器右缘内 10px），天然左右对称、不依赖文字宽度。TSX 两处组类名替换；pattern.css/colorpanel.css 旧调缝规则删除。build 通过。
- radio 贴边收尾：右选项 absolute 后不占文档流，通用 space-around 把唯一在文档流的左选项推到中间（整体偏右）；.radio-pair-230/210 组补 justify-content:flex-start，左选项圆点回左缘 10px。右选项位置用户已确认满意。
- ⚠️ radio 贴边「改动无效」根因=选择器权重被反压：通用规则 .panel-section sp-radio-group{justify-content:space-around} 权重(0,1,1)，单类 .radio-pair-230 仅(0,1,0) → 声明被吃、position:relative 因无竞争者照常生效（表象「部分生效」）。修法：新规则挂父级写成 .panel-section sp-radio-group.radio-pair-230 等四联选择器(0,2,1)。经验：改 UXP 组件通用规则时先比对权重，否则「改了没变化」极易误判为 UXP 不支持该属性。
- 间距/居中再两修：① 渐变 checkbox 与 divider/容器底间距 20px（app/图案 10px）：同 radio 的权重坑——.checkbox-grid 单类(0,1,0) 与 .row-between 同权重靠顺序险胜，渐变那处覆盖链不同即失效。改 .row-between.checkbox-grid(0,2,0) 清零外层行，并新增 .checkbox-grid .row-between/.row-start 上下 margin 0、相邻行 +10（内层行也强制，杜绝父容器差异）。② 笔刷热键竖分隔线偏左：.hotkey-entry-combo 是 flex:1 1 0%，而 .hotkey-entry-name 是 flex:1 1 auto（基准=文字自然宽→右列更宽→分隔线左移）；name 改 1 1 0% 后两列各半、分隔线居中（divider-vertical 自身 flex:0 0 auto+margin 0 8px 对称）。build 通过。
- 渐变 checkbox 上下 20px 真因（前两轮误判为权重/外边距）：渐变两个 checkbox 漏挂 .checkbox-input（app/图案/蒙版同步都有），而 .checkbox-input 里 margin:0 正是清零 UXP 原生 checkbox 默认外边距的；缺类 → 原生默认 margin 在行内上下各撑一段，行是 align-items:center，视觉上 checkbox 上下各多约 10px（divider 10+10=20、底部 10+分区 padding10=20）。已给 GradientPicker 两个 checkbox 补 className="checkbox-input"，全库 5 处 checkbox 现已全部带类。教训：UXP 原生控件（尤其 input）务必挂项目通用类，默认 margin 会伪装成「行外边距没清零」。
- checkbox 行距收口（用户把 .checkbox-input 改为 margin:10px 0 后 app 同列两行间距 30px）：叠加=上行复选框 margin-bottom10 + 相邻行 margin-top10 + 下行复选框 margin-top10。改为单一来源：.checkbox-grid 内 .checkbox-input 上下 margin 清零 + .checkbox-grid .row-between + .row-between{margin-top:15px} → 行距 15px（减半）。同时更正 .checkbox-input 注释（原写「清零」已与实际 10px 0 不符，并注明网格内被清零）。原则：一段间距只留一个来源，否则必翻倍。
- 自动关开关剔除吸管：AUTO_OFF_TOOLS(app.tsx 静态名单) 删掉 'eyedropperTool'，并同步 helpTexts.autoOffLabel（原文案列举了吸管）。名单即触发条件（handleNotification 的 select 事件→resolveSelectedTool→indexOf），剔除只需从名单移除，勿另加黑名单。
- 拖拽标签灵敏度归一化（新单一来源 src/utils/dragSensitivity.ts）：旧实现每个滑块手写 sensitivity(0.05~1)，与量程无关→「扫完量程的鼠标行程」40~510px 相差 12.8x（sigma 40px 飘、边缘阈值 510px 难拖）。公式 sensitivity=min(range/DRAG_TRAVEL_PX, step)，DRAG_TRAVEL_PX=200(≈250 面板内滑块轨道宽)；上限夹到 step 是精度守卫——clientX 为整数，1px 跨过 1 step 会有取值永远够不着(0~255 用 1.275/px 跳过 21%)。改造后行程收敛到 200~360px(1.8x)，残留差异仅来自量程>200 的精度守卫。接入点：useLabelDrag(AdjustmentPanel 13 项，删掉 config.sensitivity 字段)、DragHandler(app 6 项，顺带修 feather 上限 10→20 与 UI 不一致的 bug，旧值会卡死在 10)、ColorSettingsPanel、PatternPicker(angle/scale)、GradientPicker(角度+色标不透明度)、StrokeSetting(自带一套手写值)。全库已无手写 sensitivity 残留。
