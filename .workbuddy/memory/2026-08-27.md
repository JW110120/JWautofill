# 2026-08-27

## 删除「铅笔去锯齿」功能（被边缘平滑全覆盖）

- 删除 `src/adjustments/pencilAASmoothProcessor.ts`（SDF 覆盖率重建的铅笔去锯齿算法）。
- 删除测试/调试脚本：根目录 `pencilAA_dbg.mjs`、`analysis/pencilAA/`。
- 清理 `src/adjustments/AdjustmentPanel.tsx` 中所有铅笔去锯齿引用：import、subfeature 配置(`pencilAA`)、state、load/persist、reset、change handlers、`handlePencilAASmooth` 函数、UI 渲染块（含柔化宽度/细线保护控件）。
- **保留**「图层像素alpha采样」：`handlePencilAlphaSample` 重命名为 `handleLayerAlphaSample`，注释/提示文案改为中性描述；仍通过 flyout 菜单项 `alphaSample`（标签「图层像素alpha采样」，见 `src/utils/AdjustmentMenu.ts` + `src/utils/MenuManager.ts`）触发。
- 运行 `yarn build`（production）验证通过，重建的 `dist/bundle.js` 已无铅笔去锯齿残留（0 处引用）。
- 注意：`src/adjustments/lineSmoothProcessor.ts`（线条平滑）与部分 tooltip 中的「铅笔/去锯齿」字样属于不同功能，故意保留。
