# 2026-08-22

## 分块补色 v2：同层/分层兼顾 + 尖角补全（00:45-01:40）

**用户反馈**：① 同层场景尖角头部漏填小三角（v1 的 D≥2 判定把尖角当边缘）；② 线稿与填充分层时无法指定线稿层（v1 误删线稿参考）。

**新样本**（analysis/）：线稿.log（128×125 起点10,9）、含孔隙孔洞内部填充.log（122×117 起点13,11）、补全空隙与孔洞内部填充.log（126×121 起点11,9）。对齐到文档坐标系（起点偏移 place）分析：含孔隙 255 区完全被补全包含（无偏移污染）；补全新增 1068 像素（孔洞/缝隙/尖角）。

**关键诊断**：
- v1 漏填 = 588 个 D==1 像素（尖角应提升）+ 171 个闭掩码外（贴边豁口，close 填不掉）+ 大裂缝；
- 尖角 vs 普通边缘区分特征：8 邻域实体 ≥7（深邻居）或 原 alpha ≥121（实测 0 误伤）；普通边缘只有 1 个深邻居；
- **线稿内部区域 R = holeFill(close(Lm,1)) \ Lm 与补全 255 区吻合 98.9%**（7062 中 6982 个）→ 分层场景用 R 全提升；
- 教训：R 不能包含线稿描边本身（Lm 内 743 个像素补全=0，直接并入 M 会 MAE 崩到 17）。

**v2 算法**（blockColorPatchProcessor.ts 重写）：
- 同层：M=alpha>16 → 豁口填充（透明像素 8 邻域实体≥5，迭代）→ close(M,2) → holeFill → 距离变换 D → D≥2 提升 + 尖角（D==1 且 实体≥7 或 alpha≥80）提升
- 分层：R=holeFill(close(Lm,1)) \ Lm 内全提升（线稿内部 = 填充应覆盖区）
- 参数：maskThreshold=16, closeRadius=2, coreDistance=2, tipAlphaFloor=80, lineThreshold=16

**验证（TS 端到端）**：同层 MAE 9.07 ✓、分层 MAE 7.12 ✓（均 < 10）。同层剩余误差 = 形状增长区（补全比含孔隙略大），无线稿信息无法预知，属固有上限。

**UI**：恢复"线稿参考"下拉（默认自动=当前层上方最近像素层，getAutoLineReferenceLayer 已有），线稿层==填充层时自动退回同层。新增 readLineLayerAlphaMask（imaging.getPixels 按图层 bounds 读，映射到文档坐标）。

## 前置（22 日 00:05-00:45 的延续）
分割线（铅笔去锯齿 与 alpha对齐 之间）+ v1 分块补色恢复，见 2026-08-21.md 尾部。

## 分块补色 v5：两按钮拆分 + 算法修正（01:09-02:00）

**用户要求**：① 拆两个四字按钮（同层补色/分层补色）；② 线稿下拉用蒙版同步样式；③ 吻合率>99%、尖角吻合率>95%。

**重要 bug（v2/v4 分层实测失败根因）**：
1. **距离语义错误**：`dist_bg`（到背景距离，背景=0）被当"到填充距离"用 → 全部背景像素 cand=true → 线稿描边/背景误填（TS 分层背景 93.5%）；
2. **R\Lm 排除描边**：线稿帽顶内部的尖角头部（fill=0 good=255）被 \Lm 一起排除 → 尖角漏；
3. **Python 原型作弊**：`if good[i]==fill[i]: continue` 依赖 ground truth 掩盖 bug（MAE 7.68 假象）。

**v5 算法**（blockColorPatchProcessor.ts）：fillDist 改用 distanceToMask（掩码内=0）；cand = alpha>16 | fillDist≤1 | (线稿内部 且 联合掩码8邻域实体==9 尖角孔洞)；线稿内部 R 优先全提升；H 内 D≥2；D==1 且(实体≥7 或 alpha≥80)。

**验证（TS 端到端）**：分层 补全区 100%/背景 99.0%/尖角 100%（MAE 8.71）；同层(蛇形线) 补全区 100%/背景 98.99%/尖角 100%（MAE 5.96）。全图精确≈94%（样本过渡区偏移 167 所致，真实对齐场景不成立）。

**UI**：同层补色/分层补色 两按钮（runBlockColorPatch(sameOnly) 公共流程）；线稿下拉复用 MaskSyncSelect 组件（与蒙版同步同款：右对齐 tag、主题色背景、弹层打开自动隐藏 number 输入）；readLineLayerAlphaMask 用 imgData 实际尺寸算 comps（防静默失败）。

## 分块补色 v6：RGB 就近补色 + 凹尖角限制 + 菜单/下拉修复（07:03-07:40）

**5 项反馈修复**：
1. **凹尖角误填**：fillGaps 加 region 参数，豁口填充仅限线稿内部区域 R（分层）；凹尖角在封闭线稿外 → R 外 → 不填。同层 U 形凹槽测试 0 填充。
2. **RGB 白色**：新增 propagateColor 多源 BFS 颜色传播（从 alpha>16 像素扩散 RGB，补色取最近有值像素色）。红色 #e60e0e 样本：分层/同层 RGB 补色 100% 正确。
3. 同层线稿 alpha 提高：无独立线稿信息难根治，RGB 修复后观察。
4. **线稿下拉无法展开**：移除 onOpen={refreshLineReferenceOptions}（与可用的边缘平滑下拉一致）——选项在挂载 useEffect 刷新。
5. **菜单**：alpha 采样移到系统菜单（顺序 折叠/展开 → 隐藏/显示 → 复位顺序 → 参数复位 【-】 图层像素alpha采样）；AdjustmentMenu 新增 onAlphaSample；UI 删除 alpha采样 按钮。

**知识点**：MaskSyncSelect 的 onOpen 在 Promise 微任务中执行 + setState 可能引发下拉行为异常，不可用时去掉 onOpen（挂载时刷新即可）。

## 分块补色 v7：凹尖角不填 + 浅线/深线同层（07:48-08:30）

**用户反馈**：① 分层/同层仍填凹尖角（给了实测结果 log：分层误填 121、同层 566）；② 同层线稿颜色传播（视觉变黑）；拆 3 按钮 + 虚线分割线。

**凹尖角根因**：v6 的 R 内无脑全提升把"线稿内部 nH==9 但距填充>1px 的像素"（线稿比填充大的边缘带/凹尖角）填了。误填 121 与真孔洞 177 几何不可分，唯一区分 = **DF（距填充距离）**：误填全 DF≥2，贴边凸尖角 DF≤1。

**v7 分层规则**：R 内 fill=0 仅当 DF≤1 提升。TS 端到端：**误填=0、背景 100%、补全区 99.06%、RGB 100%**；漏 67 个 DF>1 深孔洞（尖角 86.8%，非贴边凸尖角）。

**同层浅线/深线（lineColorMode='lighter'|'darker'）**：亮度均值分界构建填充掩码（只取填充侧），线稿色不进掩码/传播。黑线+红填充合成测试：补色红 450/黑 0（零传播）。

**UI**：浅线同层补色/深线同层补色 一行 + adjustment-dashed-divider 虚线 + 分层补色单独一行（线稿下拉归它）。

## 分块补色 v6.1：回退 v7，浅线/深线改为"只过滤传播源"（08:31-08:35）

**用户反馈**：v7 的 DF≤1 限制把凸尖角也杀掉了（分层漏 67 个 DF>1 深孔洞），同层把掩码一起过滤导致缝隙吻合度下降。要求回退 V6 重改。

**v6.1 方案（几何=v6 原样，只动颜色传播）**：
- 几何掩码保持全量（含线稿）→ 闭运算/豁口/孔洞/距离判定与 v6 完全一致，缝隙/凸尖角不回退。
- lineColorMode 只构建 **colorSource（颜色传播源掩码）**：亮度均值分界，lighter 取较深侧、darker 取较浅侧，线稿像素不参与 propagateColor → 补色零线稿色。
- propagateColor 加第 6 参 colorSource?，为空 = v6 全量行为。
- 移除 v7 的"R 内 DF≤1 才提升"限制（分层恢复 R 内全提升，凹尖角误填 121 属 v6 已知基准，用户接受）。

**TS 端到端（test_v7.mjs）**：分层 补全区 100%/背景 99%/尖角 100%（=v6 基线）；同层深线 补全区 99.97%/尖角 99.61%/RGB 100%，**补色像素 红 2453/黑 0（零传播，v7 时黑 1799）**。test_v4.mjs 回归：三个场景指标与 v6 基线逐位一致。

**UI**：浅线/深线同层按钮加 `.adjustment-button-wide`（width auto + min-width 96px + padding 0 8px）；`.adjustment-dashed-divider` 改为 linear-gradient 模拟虚线（12px 周期=短线/空各 6px，避免窄容器误认实线）、margin 左右 14px 不贯通容器、opacity 0.5；按钮 title 去掉"凹尖角不填"表述（v6 行为会填）。

## 虚线不渲染修复 + analysis 清理（08:44-08:50）

**虚线修复**：UXP 对 CSS 文件里 `linear-gradient()` + `var(--border-color)` 解析不可靠（实测整条 background 不渲染，V7 的 border-top dashed 版反而能渲染）。改为 `DashedDivider` 组件：JS 用 `getComputedStyle(document.documentElement).getPropertyValue('--border-color')` 读主题色，拼内联渐变字符串（12px 周期=短线/空各 6px，`borderTop:'none'` 覆盖），CSS 保留 `border-top: 1px dashed` 作读取失败兜底。GradientPicker 的 JS 拼渐变是可行先例。

**analysis 清理**（分块补色暂告一段落，保留 27 个文件）：保留 = 8 个样本 JSON（line/fill_with_holes/fill_filled/with_holes/filled/layer_result/same_result）+ test_v4.mjs（回归）+ test_v7.mjs（当前验证）+ 验证报告 + parse3.py（log→json 解析工具）+ concave 3 个 + v7 分析 4 个（dl_analysis/dt_analysis/hfill_verify/fullmap）+ v8 系列 9 个（凹尖角深度探索，后续开发可直接续用）。删除 22 个 = 早期测试（test_ts_impl/test_v2/test_rgb/test_concave）+ 早期原型（proto1/2/final/opt/v2、parse_alpha、analyze_alpha/diff、align/classify/tipmap、visualize/zoom/show3/show_all）+ 3 个 png。

## 虚线二修：纯 DOM 短线方案（08:52-08:55）

用户实测渐变方案仍不行：`background-repeat: repeat-x` 在 UXP 不生效，背景只渲染一次（12px tile 居中 → 面板中间一根 ~10px 短线）。根因 = UXP 对 background 高级特性（渐变/background-size/repeat）整体不可靠。

**最终方案（纯 DOM）**：DashedDivider 渲染 60 个 6px 短线 span（margin-right 6px = 12px 周期），容器 flex row + overflow hidden + opacity 0.5，短线背景色用 state 存主题 --border-color（useEffect 读 getComputedStyle）。零背景/边框依赖，UXP 必支持。已 build 通过。

## 下拉宽度与间距（08:57-09:00）

".adjustment-smooth-mode-select"（平滑模式 + 分层补色线稿参考 两处共用）：宽度 170→190px（flex 0 1 190px 可收缩），去掉 margin-left:auto 右对齐，改 margin-left:3px 紧跟标签（item gap 3px + 3px = 与标签总间距 6px）。"平滑模式"标签补 adjustment-slider-label-4（原 20px 基础标签 4 字会溢出破坏间距）。已 build 通过。

## 下拉间距修正为 8px（09:03-09:06）

用户实测"平滑模式"下拉与标签紧贴 → 根因：**UXP 不支持 flex gap**，item 的 gap:3px 不生效，只有 select margin-left 3px。修复：`.adjustment-smooth-mode-select` margin-left: 3px→8px；新增 `.adjustment-slider-item-no-gap { gap: 0 }` 修饰类加到两行 item（gap 存在时置 0 抵消，不存在时无副作用），保证两种渲染环境下间距都精确 8px。已 build 通过。

## 下拉行上下边距 10px（09:07-09:10）

新增 `.adjustment-slider-container-vpad { margin: 10px 0 }` 修饰类，加到"平滑模式"与"线稿参考"两行的 adjustment-slider-container 上（覆盖默认 margin-bottom 3px），保证与上下元素各 10px 间距。已 build 通过。

## 平滑模式→中间值半径 内部间距（09:09-09:12）

用户反馈 vpad 容器 margin 没生效于"平滑模式→中间值半径"——根因：中间值半径行在 **vpad 容器内部**（容器 2852 内含平滑模式行+条件渲染的后续行），容器 margin 只作用于容器外。修复：新增 `.adjustment-slider-item-gap-bottom { margin-bottom: 10px }` 加到平滑模式 item（2853），容器内两行间距=10px（edge/line 两模式都覆盖）。已 build 通过。
