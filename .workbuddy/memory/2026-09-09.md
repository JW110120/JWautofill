# 2026-09-09

## 新建 Skill：uxp-frontend-spec（替代 uxp-themeable-dropdown）
- 位置：`.workbuddy/skills/uxp-frontend-spec/`（项目级），已删除旧技能 `uxp-themeable-dropdown`。
- 内容从 common.css / app.css / input-fix.css / adjustment.css / adjustment-input.css / theme.ts 提炼：
  - `SKILL.md`：三条铁律（单一来源 / 加载顺序 / 禁 HEX）、四主题令牌表、尺寸公式表、
    面板高度链、滑块行标准 DOM、状态样式规则、新建面板 checklist、反模式清单。
  - `references/component-catalog.md`：12 组通用类目录 + 可复制 TSX/CSS 片段 + 面板 CSS 文件职责表。
  - `references/uxp-pitfalls.md`：15 条已验证 UXP 坑（原生控件穿透与 !important 反转、var() 不稳、
    Provider 断高度链、无 gap、禁 outline、range step 失效、sp-radio 影子布局等）+ 排查顺序。
  - `assets/panel-template.tsx`：新面板骨架（分区/滑块行/下拉/radio/预设网格/折叠/浮层/通知）。
- 关键结论补充：manifest 面板宽 250 → `.panel` padding 10×2 → 可用宽 230（radio-pair 档位 230/210）。

## 修复：快速合并图层弹出「易修: 命令"获取"当前不可用」2~3 次
- 根因：PS 通知事件（set/select/make/delete）在命令执行【中途】派发；Ctrl+E 合并图层时
  app.tsx handleNotification→checkMaskModes 与 PatternPicker/GradientPicker/ColorSettingsPanel
  的同名检测【立刻】发 batchPlay get，撞上 PS 忙碌窗口，宿主弹原生报错框。
  该弹框 JS try/catch 与 dialogOptions:'dontDisplay' 都拦不住（先例：AdjustmentPanel
  runWithTemporaryUnlock 注释里的 applyLocking「命令"加锁"当前不可用」）。与图层同名无关。
- 修复：新增 `src/utils/psProbe.ts` debouncePsProbe（200ms 尾沿防抖 + cancel），
  四处通知触发的蒙版/通道探测改为防抖执行；app.tsx getSelection 与 AdjustmentPanel
  getSelectionBounds 两处裸 get 补 dialogOptions:'dontDisplay'。webpack 构建通过。
- 待验证：UDT Reload 后快速合并图层不再弹框（若 MaskSyncEngine 开启时仍弹，下一步给
  其 scheduleSync 的 200ms 提到 300ms+ 或在 doTimedSync 前探测忙碌态）。
