# 2026-08-30

## 统一下拉背景 + 状态通知设计语言
- theme.ts：新增 `--dropdown-bg-color` 四主题固定值（darkest #1a1a1a / dark #393939 / light #dadada / lightest #ffffff），所有下拉（sp-picker、.mask-sync-select-*、BrushSelect）引用该变量即统一。
- 新增 `--notify-ok/fail/warn` 三态令牌（fg/bg/border），深主题亮色、浅主题深色，保证反差可读性。
- adjustment.css：`.mask-sync-status-bar`（引擎状态 + 守护进程就绪）背景改 `var(--bg-color)`；`.mask-sync-result`(.ok/.fail) 改用令牌 + 1px 描边，新增 `.warn`；试用横幅 `.is-trial` 同用 `--notify-ok-*`。
- BrushHotkeySection.tsx：底部文字通知由内联样式改为共用 `.mask-sync-result` 类名（ok/warn），与蒙版同步同款。
- 已写入 MEMORY.md「UI 设计语言」约定，禁止硬编码这类颜色。
- 补充一致性：试用横幅 `.trial-expired` 红字、`is-expired` 红底也改用 `--notify-fail-*`（light 主题下亮红 #ff6b6b 在浅灰上看不清，改深色令牌后反差正常）。
- `yarn build` 通过（仅体积警告）。

## 主面板下拉统一为自绘 Select（替换 sp-picker）
- 新增 `src/components/Select.tsx`（通用自绘下拉，复用 `.mask-sync-select-*` CSS，支持分组/分隔线/禁用/选中高亮），与调整面板下拉、BrushSelect 视觉完全一致。
- adjustment.css：新增 `.mask-sync-select-head.disabled`、`.mask-sync-select-divider`；更新注释（原"与主面板 sp-picker 统一"→"统一设计语言"）。
- 替换 4 处 `sp-picker`→`Select`：app.tsx 混合模式（groups+disabled）、GradientPicker 渐变类型、PatternPicker 缩放（className="zoom-picker"）、StrokeSetting 混合模式（groups）。主因：sp-menu 展开菜单背景 UXP 下无法 CSS 覆盖。
- styles.css：删除失效的 sp-picker/sp-menu 背景规则，改为注释说明改用自绘组件。
- 因 #app 与 #pixeladjustment 同文档同 bundle，adjustment.css 全局可用，无需重复引入。

## darkest 下拉背景改为 rgb(32,32,32)
- theme.ts：darkest `--dropdown-bg-color` 由 #1a1a1a 改为 `rgb(32, 32, 32)`（四主题现为 darkest rgb(32,32,32)/dark rgb(57,57,57)/light rgb(218,218,218)/lightest rgb(255,255,255)）。

## 全代码 HEX 颜色转 RGB（硬性约定）
- 写脚本将 src 下 9 个 .css/.ts/.tsx 中所有 HEX 颜色字面量（约 66 处）转为 rgb()/rgba()；转换后 grep 确认无残留 HEX、无 url(#id) 误伤；`yarn build` 通过。
- 约定：以后颜色代码只能用 rgb()/rgba()，禁止 HEX，已写入 MEMORY.md「设计语言」并单列。

## MEMORY.md 重组为两大块
- 一、设计语言与设计规范（颜色表示法/下拉规范/UI约定/架构结构/提交约定）；二、避坑指南（UXP/React、算法、守护进程、跨面板热键、批量删除、sp-menu 不可改背景）。
- `yarn build` 通过（仅体积警告）。

## 收尾：BrushSelect 复用通用 Select（消除重复）
- `Select.tsx` 增加可选 `tag?: React.ReactNode`（每选项右侧标注，笔刷类型图标用）；头部与弹出行均渲染 tag。
- `BrushSelect.tsx` 重构为薄封装：把 `{value,main,tag}` 映射为 `SelectOption[]` 后委托 `Select`；公开接口不变（BrushHotkeySection 调用点零改动）。
- 现全插件所有下拉（主面板 4 个 + 调整面板 N 个 + 笔刷下拉）统一走 `Select` + `.mask-sync-select-*` CSS，单一实现、单一设计语言。
- `yarn build` + `tsc --noEmit`（仅 grep 改动文件）均零报错。

## 收尾 2：下拉选项 ellipsis 修复 + 沉淀 Skill
- adjustment.css：`.mask-sync-select-opt-main` 补 `white-space: nowrap`（否则 `text-overflow: ellipsis` 不生效，长笔刷名/混合模式名会换行而非省略）。
- 新建项目技能 `.workbuddy/skills/uxp-themeable-dropdown/SKILL.md`：沉淀「UXP 下拉必须用自绘 Select 替代 sp-picker/sp-menu（flyout 背景无法 CSS 改）、颜色仅用 rgb/rgba」的可复用工作流；已删除脚手架示例文件。
- `yarn build` 通过（仅体积警告）。
