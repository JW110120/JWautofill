# 2026-09-01

## 滑块容器套娃重构（为精确控制间距）
三个面板的滑块统一改为三层容器结构：`slider-parameter-collection`（标签+数值输入+单位符号，同一行）→ `entire-slider`（parameter-collection + RangeSlider 滑块本体）→ 面板级 group 容器。

- **选区填充面板**（`src/app.tsx` 不透明度+羽化两个滑块）：原 `.slider-container` 保留作为卡片样式类，叠加新组类 `selection-fill-slider-group`；内部两个滑块各包一层 `.entire-slider` > `.slider-parameter-collection`。styles.css 补了 `.entire-slider`/`.slider-parameter-collection { width:100% }` 结构规则。
- **图案面板**（`src/components/PatternPicker.tsx` 角度+缩放）：沿用原有 `pattern-setting-item-group`（组）与 `pattern-setting-item`（外壳），在 item 内新增 `.pattern-entire-slider` > `.pattern-slider-parameter-collection`。
- **纯色面板/颜色动态设置**（`src/components/SliderControl.tsx`，四个滑块共用此组件）：原 `.colorsettings-slider-header` 升格更名为 `.colorsettings-slider-parameter-collection`（colorpanel.css 同步改选择器），新增 `.colorsettings-entire-slider`（flex column）包裹 collection+RangeSlider；外层 `colorsettings-slider-item` 与 `colorsettings-slider-group` 保留。禁用态选择器均为 item 后代选择器，嵌套加深后仍命中。

### 关键避坑
- pattern.css 原有 `.pattern-setting-item div{display:flex}` 通配后代选择器会把新容器压成横向排列 → 必须加 `.pattern-setting-item .pattern-entire-slider` / `.pattern-slider-parameter-collection` 的 flex-direction:column 定向覆盖（已写入 pattern.css 并注释原因）。
- selection-fill 的 collection 保持 block（不能改 flex row）：`.slider-label` 自身是 flex space-between 且依赖块级 100% 宽度实现「文字左、数值右」，collection 若为 flex row 会让 label 收缩成内容宽、破坏布局。

## 二次调整（同日晚）
- **容器层级修正**：slider-container 改为 selection-fill-slider-group 的**外层**容器（不再并列同 div）；selection-fill-blend-mode-container 移入 slider-container 内部、slider-group 外部，CSS 由 `margin-bottom:-10px` 改为 `margin-top:-10px`（骑在卡片上边缘）。
- **数字输入统一**：新增全局类 `.slider-number-input`（styles.css，width:46px + z-index:1，外观由 `#app input[type="number"]` 提供），挂到 7 处输入：主面板不透明度/羽化、选区选项平滑/锐度/扩散、描边面板宽度/不透明度（描边面板实际有 2 个，用户说 6 个）。移除了这些输入的内联 width:30px/24px；同步删除会以更高优先级覆盖类宽度的旧规则：`.selection-slider-item input[type="number"]` 的 44px、`.stroke-wide-container input[type="number"]` 的 42px。保留各处 marginLeft 负值/ textAlign 等布局补偿为内联样式。

## 三次调整（同日）
- 去掉滑块数字输入的内联布局补偿：选区选项 3 处 `marginLeft:-10px`、描边宽度 `marginLeft:-5px`、描边两处 `textAlign:center` 全部移除（只留 `.slider-number-input` 类；描边宽度输入的居中仍由 stroke.css 规则提供）。
- 回退混合模式容器改动：`selection-fill-blend-mode-container` 移回 slider-container **外部**（恢复兄弟关系），CSS 恢复 `margin-bottom:-10px`（用户实测在内部骑上边缘的方案不要）。
- **两面板全部数字输入宽度统一 35px**：提交 94266de40eaf 为修 UXP number 输入绘制越界/三位数显示不全把输入框拉宽（46px 等），实测过宽。改了 6 处宽度规则：styles.css `.slider-number-input` 46→35、colorpanel.css colorsettings 40→35、pattern.css 46→35、gradient.css opacity-input 54→35 + gradient-setting-item 46→35、adjustment.css `.adjustment-number-input` width 46→35 且 **flex-basis 46px→35px 必须同步**（工具箱输入是 flex item，basis 不同步会仍占 46px）。

## 四次调整（编译报错修复）
- **JSX 失衡根因**：第二轮把 `slider-container selection-fill-slider-group` 合体 div 拆成两层时，多开了一层 `<div>` 但没补 `</div>`；原合体版里两个尾部 `</div>` 分别闭合合体容器和 `selection-fill-container`，拆分后它们变成了闭合 group 和 slider-container，导致 `selection-fill-container` 失去闭合标签（TS17008 根 div 未闭合 + 1914 行连环 TS1381）。
- **修复**：在第二整个 entire-slider 闭合后、expand-section 前，为 `selection-fill-container` 补一个 `</div>`。webpack 编译通过（仅 bundle 体积 warning）。
- **教训**：拆分/包裹容器时必须用逐行 div 开闭计数脚本核对（对比 git index 基线），嵌套层级深的 UXP JSX 靠目测必然翻车。

## 五次调整（滑块行容器语义定稿）
三面板滑块统一为「行容器装 文字标签(70px) + 数字输入 + 单位符号」结构，ew-resize 光标归行容器：

- **styles.css**：`.slider-text` 只留文字样式（font-size/color/display:block/width:70px/flex:none）；`.slider-parameter-collection` 接管布局（width:calc(100%-20px) + margin:0 auto + flex row space-between + align-items:center）；拖拽光标类改为 `.slider-parameter-collection.dragging/.not-dragging`（原 `.slider-label.*` 是死规则，JSX 用的是 slider-text）。
- **app.tsx**：不透明度/羽化两个滑块 JSX 重构——dragging/not-dragging 类从 label 移到 `.slider-parameter-collection`，label 只包文字（`className="slider-text"`），数字输入+单位 div 移出 label 成为行容器直接子元素。
- **pattern.css**：删除 `.pattern-setting-item label` 旧规则，新增 `.pattern-setting-item .pattern-slider-text`（70px 文字标签）；`.pattern-slider-parameter-collection` 由 flex column 改 flex row + width:calc(100%-20px) + margin:0 auto + cursor:ew-resize；`.pattern-angle-range/.pattern-scale-range` 由 210px+margin-left:6px 统一为 calc(100%-20px) + margin:-5px auto 0（与行容器对齐）。
- **PatternPicker.tsx**：角度/缩放两个 label 加 `pattern-slider-text` 类、删内联 cursor 样式（恒为 ew-resize 的三目），input+span div 移出 label。
- **colorpanel.css**：`.colorsettings-slider-parameter-collection` 补 width:calc(100%-20px)+margin:0 auto+cursor:ew-resize（原 margin-left:8px 作废）；新增 `.colorsettings-slider-text`（70px）；label 的 hack margin（-3px 左 / 5px 下）删除；禁用态选择器 `.colorsettings-slider-item.disabled label` → `.disabled .colorsettings-slider-text`、`.colorsettings-slider-label.disabled` → `.colorsettings-slider-text.disabled`；`.colorsettings-slider-range` 宽度 100% → calc(100%-20px) 居中。
- **SliderControl.tsx**：label 类 `colorsettings-slider-label` → `colorsettings-slider-text`，dragging/not-dragging 移到行容器。
- webpack 构建通过（仅体积 warning）。注意：Git Bash 下 `yarn` 壳损坏（指向不存在的 npm 全局路径），直接用 `node ./node_modules/webpack/bin/webpack.js --mode=production` 构建。

## 六次调整（清除模式下混合模式禁用态变灰修复）
**现象**：APP 面板开启清除模式时，「混合模式：」标签与下拉文字没有变灰。
**根因（三重）**：
1. 旧规则类名 `.blend-mode-label.disabled` 与 JSX 的 `selection-fill-blend-mode-label` 不匹配（死规则）；用户手改了类名但仍是 `opacity: 0.5` 而非 `--disabled-color`。
2. dist 是旧构建——手改 CSS 后没重新构建，插件加载的是 dist 里的 bundle。**改样式后必须重新构建才生效**（构建命令见上）。
3. adjustment.css 的 `.mask-sync-select-head.disabled`（下拉禁用态，Select.tsx 与调整面板 MaskSyncSelect 共用）也是 opacity 方案。
**修复**：
- styles.css：`.selection-fill-blend-mode-label.disabled { color: var(--disabled-color) }`（弃 opacity）；同时把用户误挂在 `.switch-container:has(sp-switch[disabled])` 上的「混合模式」注释还原——该规则的真实用途是「新建图层模式开启时清除模式开关被禁用→开关标签变灰」（清除模式开关目前仍是 sp-switch，未迁到 CustomSwitch）。
- adjustment.css：`.mask-sync-select-head.disabled` 由 `opacity:0.5` 改为 `color: var(--disabled-color)`（value/caret 继承 color，caret svg 是 currentColor 自动变灰；两面板禁用下拉观感统一）。
- 构建产物验证：`dist/bundle.js` 内嵌 CSS 与拷贝的 `dist/styles.css` 均含新规则。

## 七次调整（滑块标签宽度算法移植 + 按钮宽度算法统一）
**滑块标签宽度算法（工具箱→APP 三面板移植）**
- 算法来源 `src/adjustments/adjustment.css`：`W(n)=20+(n−2)×13.33` → 2/3/4/5/6字=20/33/47/60/73px（锚点 2字20px、5字60px 线性插值）。
- styles.css / pattern.css 已含 `-2`~`-6` 修饰类（用户此前已建）；本次补全 colorpanel.css：去掉 `.colorsettings-slider-text` 的 `width:70px`，加 `-2`~`-6`（20/33/47/60/73px），并改 `SliderControl.tsx` 按 `label.length` 自动挂 `-N`（clamp 2~6）；pattern.css 补齐 `-2/-4/-5/-6` 与另两面板对齐。

**按钮宽度算法（工具箱）**
- 新算法：**width = 字数 × 字号 + 20px，按钮容器无内边距（padding:0）**。普通档 font13 → 13n+20（2→46、4→72、6→98、8→124）；紧凑档 font12（守护进程状态条）→12n+20（7字→104）。
- adjustment.css：`.adjustment-button` 80→72(去padding)、`.lv2` 64→46、`.wide`→124兜底，新增 `-5/-6/-7/-8`=85/98/111/124；`.auto` 去 padding、`.compact` 设 width:104(font12,7字)。
- AdjustmentPanel.tsx 六个 wide 按钮按实际字数重挂：-8(alpha下/上对齐)、-7(浅/深线同层补色)、-5(保底下对齐)、去掉 wide 回默认(线条加黑,4字)。
- webpack 构建通过（仅体积 warning）；新规则已确认进 dist/bundle.js。

## 七次调整（纯色面板 SliderControl 合并进 ColorSettingsPanel）
**现象**：用户反馈颜色面板（纯色动态设置）无法渲染，怀疑是上一轮给 SliderControl 加的「按 `[...label].length` 动态拼宽度类名」的缩写写法导致（命名也易误认为这是管理所有滑块的总文件）。
**处理**：
- 删除 `src/components/SliderControl.tsx`（仅被 ColorSettingsPanel 引用，已确认无其它引用）。
- `ColorSettingsPanel.tsx`：改为 `import RangeSlider`（原由 SliderControl 内部引入）；组件内新增 `renderSlider(settingKey,label,value,min,max,unit,widthClass)` 帮助函数，宽度修饰类**显式传入**（不再按字数算）；JSX 里把 4 个滑块逐一列出：灰度抖动(-4)/色相抖动(-4)/饱和度抖动(-4)/亮度抖动(-4) 在条件分支，不透明度抖动(-5) 恒显。widthClass 取值沿用工具箱标签算法（4字=47px→-4、5字=60px→-5）。
- webpack 生产构建通过，dist 无 SliderControl 残留、显式宽度类已进 bundle。

## 八次调整（纯色面板标签换行修复）
**现象**：颜色面板 4 字标签（灰度/色相/饱和度/亮度抖动）会折行，用户疑是字号问题。
**诊断**：不是字号——纯色面板标签同为 13px，与工具箱/APP/图案一致。根因是 `.colorsettings-slider-text` 漏了 `white-space: nowrap`；而另两面板与工具箱的滑块文字标签规则都带 `white-space: nowrap`，盒子略窄时只溢出不折行。宽度算法 `W(n)=20+(n-2)×13.3`（4字=47px）对 4 个约 52px 宽的汉字本就略窄，但这是用户既定算法、工具箱同值已接受（仅溢出发丝级）。
**修复**：`colorpanel.css` 的 `.colorsettings-slider-text` 补 `white-space: nowrap`（与 `.slider-text`/`.pattern-slider-text`/`.adjustment-slider-label` 一致）。构建通过，已进 dist/bundle.js。

## 九次调整（单位符号容器统一 14px + 单行滑块条长度随行宽自适应）
**需求**：① 所有单位符号容器宽度统一 14px 且 `white-space:nowrap`；② 单行滑块（标签+滑块条+数字输入+单位符号 同行）的滑块条长度要随「标签字数 × 行容器宽度」综合得出，固定 140px 会把两字标签挤换行；③ 单行行容器内 4 个容器垂直居中对齐、各自内部元素左对齐+垂直居中。
**单位符号 14px（no-wrap）落地位置（五面板 + 两行滑块）**：
- APP 两行（不透明度/羽化）inline-styled `<span>` → 新增 `.slider-parameter-collection > div > span{width:14px;flex:none;nowrap}`。
- APP 单行 `.selection-slider-item span`：原已 14px，补 `nowrap;flex:none`。
- 图案两行 → 新增 `.pattern-slider-parameter-collection > div > span{14px;nowrap}`（**必须限定此选择器**，原 `.pattern-setting-item span` 通配会命中平铺/单次 `.pattern-radio-item-label` 把选项文字压窄）。
- 纯色两行 `.colorsettings-slider-item span`：8px→14px + nowrap + flex:none + 居中。
- 渐变角度 `.gradient-setting-item > div > span`：补 14px + nowrap + flex:none + 居中（保留 margin-left:-4px）。
- 绘画工具箱 `.adjustment-unit`：15px→14px + nowrap + `display:flex;align-items:center`。
**单行滑块条长度**：`.selection-slider-input` 由 `width:140px` 改为 `flex:1 1 auto;min-width:0`（占满行剩余宽 = 行宽 − 标签 − 数字输入35 − 单位14 − 间距）；渐变 `.gradient-angle-range`、工具箱 `.adjustment-slider-input` 本就 flex:1，无需改。`.selection-slider-label` 补 `white-space:nowrap;flex:none` 防压缩换行（用内容宽而非 -N，避免 2 字标签文字溢出 20px 窄盒压到滑块）。
**对齐**：各单行行容器本就 `align-items:center`；补 `.adjustment-slider-label`/`.wide`/`.wider` 的 `align-items:center`，单位 span 统一 `display:flex;align-items:center`（内部文字左对齐+垂直居中）。
**构建**：webpack 生产构建通过；`dist/bundle.js`（style-loader 内联，UXP 实际加载源，含 @import 合并）与 `dist/styles.css` 均已含新规则（注：dist/styles.css 的 @import 兄弟文件未随包拷贝，面板样式实际由 bundle.js 提供）。

## 十次调整（5 项 UI/行为修复：主按钮 hover、齿轮按钮统一样式、加载不切笔刷、同名笔刷精确选中、number input 防越界）
**问题 1 — 主面板按钮 hover 加边框高亮**（与工具箱一致）
- styles.css `.main-button:hover` 在 `background-color:var(--hover-bg)` 基础上补 `border-color: var(--primary-color)`（line 320）。

**问题 2 — 齿轮按钮统一为工具箱垃圾桶样式（无边框图标按钮）且垂直居中**
- styles.css 在 `.icon-fill` 后新增 `.hotkey-icon-button` 全套样式（移植自 adjustment.css）：透明背景、无边框、24px、图标用 `--text-color`、hover 变蓝、active 缩放 0.94、disabled 灰显（line 164-201）。
- app.tsx：描边设置 + fgSettings/patternSettings/gradientSettings 共 4 个 `sp-action-button quiet` 齿轮按钮改为 `<IconButton>`（已 import，line 18；line 1605/1647/1659/1671）。IconButton 内部 `align-items:center` 保证垂直居中。

**问题 3 — 插件加载不切换笔刷**
- 根因：`loadBrushes(false)` 初始化会调 `detectAllBrushTypes()`（内 `selectBrushForDetection`+`restoreCurrentBrush` 短暂切到"散布"笔刷）。
- 修复：`loadBrushes(detect=false, notify=false)` 仅枚举笔刷名、不检测类型、不碰当前笔刷；插件加载改 `loadBrushes(false, false)`（line 92），手动刷新仍 `loadBrushes(true)`（line 544，detect 默认 true）。

**问题 4 — 同名笔刷按精确序号选中（不再总选中列表较上方那支）**
- HotkeyBridge.ts：`HotkeyEntry` 加 `brushIndex?:number`（line 16）；`selectBrushCommands(brushName, brushIndex?)` 在 brushIndex 为数字时用 `{_ref:'brush',_index:brushIndex}` 精确选中第 N 支，失败回退按名（line 387-410）；`applyBrush` 透传（line 411-422）；`handleHotkey` 按 `combo` 反查条目拿 brushIndex 后传给 `applyBrush`（line 302-305）。
- BrushHotkeySection.tsx：录制保存条目时存 `brushIndex: selectedKey ? Number(selectedKey) : undefined`（line 258），selectedKey 即下拉唯一索引（=列表索引）。

**问题 5 — number input 所有分辨率不越界（含 3440×1440 带鱼屏 100% 缩放）**
- 现象：公司带鱼屏（100% 缩放）number input 下越界，4K（高缩放）正常；激活码输入已用 `.license-input-wrap` 裁剪容器解决。
- 修复：styles.css 新增 `.num-input-row` 裁剪容器（思路同 `.license-input-wrap`）：用 `#app` 前缀提高特异性盖过 `#app input[type="number"]`；wrap 固定高 32px + `overflow:hidden` + 边框背景画在 wrap 上，input 改无边框 24px 居中、wrap 的 `overflow:hidden` 裁掉原生绘制区溢出（line 110-140）。
- 7 处 number input 外层改 `<div className="num-input-row">`：app.tsx 不透明度(1319)/羽化(1357)/平滑(1415)/锐度(1450)/扩散(1484)，StrokeSetting.tsx 宽度(126)/不透明度(186)。

**验证**：tsc --noEmit 无新增报错（列出的均为预存在 uxp/photoshop 模块声明缺失、`sp-*` 自定义元素、IconButton onKeyDown 类型签名等环境/既有问题，无一条指向本次改动）。grep 已交叉核对全部落点。
**待办**：改了 CSS/样式，必须 `node ./node_modules/webpack/bin/webpack.js --mode=production` 重新构建，`dist/` 仍是旧产物、PS 加载的是 dist bundle，不构建不生效。

## 十一轮调整（用户回批 3 项修复的再修正）
**② 齿轮按钮垂直居中对齐（误解"垂直居中"）**
- 用户原意：填充模式里三个齿轮的中心高度要与"纯色"文字中心对齐（像 PS 那样行内垂直居中），不是要齿轮和文字堆叠成同一列。
- 根因：`.hotkey-icon-button` 是 `display:flex`（块级 flex 容器），在 `sp-radio` 的 label 里会另起一行，导致齿轮掉到文字下方、左对齐（"同一列"）。
- 修复：`.hotkey-icon-button` 改 `display:inline-flex` + `vertical-align:middle`（styles.css line 164），在 `sp-radio` label 内与文字行内并排且垂直居中；在 flex 容器（垃圾桶/工具箱）里 inline-flex 仍被当作 flex item，无副作用。

**④ 同名笔刷：面板显示角标 + 按精确笔刷选中**
- 用户回批：录下的条目在面板上仍显示"绸光涂抹"而非"绸光涂抹（1）"，且快捷键呼出的不是对应笔刷。
- 显示修复：新增 `brushDisplay(brush, brushIndex, brushes)`（按列表序号反推同名项排位，第 1 支无后缀、第 2 支起 ` (N)`，N=排位-1，与下拉 brushOptions 同算法）；面板条目列表（line 615 的 `hotkey-entry-name`）与保存提示均改用它。
- 选中修复（核心）：原 `_index` 方案不可靠——PS 的 brush 引用按 `_name` 只选最上方同名项、`_index` 又依赖枚举顺序不一定等于面板顺序。改为按笔刷预设**唯一 id** 精确选中：
  - `enumerateBrushes()` 返回类型由 `string[]` 改为 `Array<{name; id?}>`（id 取自 `app.brushes.get()` 的 `id/_id/localID` 或 presetManager 的 `_id/id`）；`detectAllBrushTypes` 与 `BrushHotkeySection.loadBrushes` 同步改为取 name/id 两列（`brushIds` state）。
  - `HotkeyEntry` 加 `brushId?`；录制存 `brushId: selectedKey ? brushIds[Number(selectedKey)] : undefined`；`selectBrushCommands` 优先级 `_id` → `_index` → `_name`，`applyBrush`/`handleHotkey` 透传 `brushId`。`reRecordEntry` 用 `{...e, combo}` 自然保留 brushIndex/brushId。
  - 旧配置（无 brushId）回退到 `_index`/`_name`，无回归。
- **避坑**：`export interface BrushInfo` 与 photoshop 全局 ambient 类型同名冲突（全局 BrushInfo 带 toolEnum/type/diameter，导致 `BrushNameId` 也报缺这三项、疑似 TS 解析串台）。最终去掉具名 interface，直接用内联返回类型 `Array<{name; id?}>` 规避。

**⑤ number-input 再修：单位独立于容器外 + 降低高度 + 数字居中 + 工具箱一起改**
- 用户回批：① 单位符号和数字被我塞进同一裁剪容器（不要）；② 容器 32px 比原本 22px 高太多、数字偏上不居中；③ 工具箱 number-input 没一起改。
- 结构：裁剪容器 `.num-input-row` 只包 `<input>`，单位 `<span>` 移到外层 `.num-input-wrap`（flex 行、垂直居中）作兄弟；主面板 7 处（app.tsx 不透明度/羽化/平滑/锐度/扩散 + StrokeSetting 宽度/不透明度）与工具箱 13 处（AdjustmentPanel 用 node 正则把 `<input className="adjustment-number-input">` 包进 `.num-input-row`）全部重构。
- 样式：`.num-input-row` 高度 32→30、input 24/line-height:24（line-height=框高保证数字垂直居中，不再偏上）；新增 `.num-unit`（margin-left:4px、独立于裁剪框外不被裁）；`#pixeladjustment .num-input-row` 复用同套裁剪（并覆盖 `.adjustment-number-input` 去边框、height/line-height 24，边框改画在 wrap 上）。
- 工具箱 `.unit-container` 本就单位独立（`.adjustment-unit`），此次只把 input 包进裁剪容器，达成与主页一致且不溢出。

**验证**：`tsc --noEmit` 改动文件无新增报错（其余均为预存在 uxp/photoshop 模块缺失、`sp-*` 自定义元素、ColorSettings 字段等环境/既有问题）；生产构建通过；grep 确认 dist/bundle.js 含 `display:inline-flex`、`.num-input-wrap`、`.num-unit`、`#pixeladjustment .num-input-row`、`_ref:"brush",_id`（brushId 选中）、`brushDisplay` 逻辑。

---

## 第九轮：齿轮排版终修 + 同名笔刷支持整体回退 + number-input 27px 定稿（17:15）

**② 填充模式齿轮排版（终修，推翻 inline-flex 方案）**
- 用户截图回批：齿轮仍在文字下一行，inline-flex 方案无效。
- 根因再判：`sp-radio` 的 slot 内对 div 子元素的行内流布局不可依赖（inline-flex 也被换行）。改用**绝对定位**绕开 slot 布局：`.radio-item{position:relative}` + `.radio-item .hotkey-icon-button{position:absolute;right:0;top:50%;margin-top:-12px}`（行高 32、按钮 24 → 垂直居中）；`.radio-item-label{margin-right:28px}` 给齿轮让位。position:absolute/relative 在本项目多处已验证可用（colorpanel/gradient/pattern/adjustment.css）。
- 教训：**sp-radio slot 内不要放块级自定义按钮并指望行内排——直接绝对定位到行右缘最稳。**

**④ 同名笔刷支持整体回退（用户放弃该功能）**
- 用户决定不支持同名笔刷，要求删掉多轮尝试的代码并在手册强调。
- HotkeyBridge.ts：`HotkeyEntry` 删 `brushIndex`/`brushId`（brush 注释标注"不支持同名笔刷"）；`selectBrushCommands`/`applyBrush` 恢复仅按 `_name`；`handleHotkey` 删 combo 反查；`enumerateBrushes()` 恢复返回 `string[]`（`detectAllBrushTypes` 调用点同步）。
- BrushHotkeySection.tsx：删 `brushDisplay` 函数、`brushIds` state；录制条目只存 `{combo,action,brush}`；列表显示恢复 `e.brush`。下拉里的 `(1)/(2)` 同名角标是更早就有的展示逻辑，保留（能提示用户存在同名，配合手册说明）。
- 手册同步：README「快捷键服务」加 ⚠️ 不支持同名笔刷条目；docs/toolbox-guide.html 笔刷热键章节加 `callout warn`（同名只选最上方那支，请重命名），故障排查标题改为「绑了笔刷热键但切换不了？/ 切换的不是我要的那支？」。

**⑤ number-input 27px 定稿**
- 用户回批：30px 仍偏高、数字仍偏上。
- 定稿方案：wrap 27px，**input 高度撑满 wrap（27px）+ line-height:27px**——不依赖 flex align-items 对原生 input 的居中（实测原生控件贴顶导致数字偏上），与激活码 `.license-input`（line-height=height）同一套已验证居中方案；底部原生溢出仍由 wrap overflow:hidden 裁掉。主面板 `#app` 与工具箱 `#pixeladjustment` 两套同步改。

**验证**：tsc 改动文件无新增报错；生产构建通过；grep 确认 bundle 含 `height: 27px`、`position: relative`、`margin-top: -12px`，且 `brushDisplay`/`brushIndex`/`_id: brushId` 已为 0 处；两文档均含「不支持同名笔刷」。

---

## 第十轮：齿轮 3px 偏高 + 计算方法 radio 崩坏修复 + 下拉去重回退 + number-input 缓冲方案（17:45）

**② 齿轮偏高 3px + 颜色面板「计算方法」崩坏成两行（上一轮绝对定位方案的连带 bug）**
- 齿轮偏高：`margin-top:-12px`（几何居中）视觉偏高 3px → 定稿 `-9px`。
- 计算方法崩坏根因：`.radio-item-label` 是全局类，颜色面板 ColorSettingsPanel 的「绝对/相对」radio 也用它；上一轮给全局 `.radio-item-label` 加的 `margin-right:28px` 在 flex:1 + space-between 的 sp-radio 里把内容挤出单行 → 两行崩坏。
- 修复：全局 `.radio-item`/`.radio-item-label` 全部还原；齿轮规则 scoped 到 `.fill-mode-group .radio-item`（position:relative / absolute / -9px），label 让位 margin 也 scoped 到 `.fill-mode-group .radio-item-label`。
- **教训：公共类（.radio-item-label 等）上加布局属性前必须全局 grep 使用方；面板专属布局一律 scoped 到面板容器类。**

**④ 下拉回退到每个笔刷名只显示一次（用户指定参考 f5b7b5a 时代的行为）**
- `brushOptions` 改为 `Array.from(new Set(brushes))` 去重，value 直接用笔刷名（去重后唯一），删除 (1)/(2) 角标与索引反查；onChange 直接 `setSelectedBrush(v)`；selectedKey 同步 effect 简化为 `setSelectedKey(selectedBrush)`。
- 至此同名笔刷相关代码（brushIndex/brushId/brushDisplay/角标/索引 value）全部清除。

**⑤ number-input「撑满容器」方案被实测推翻，改回缓冲方案**
- 用户实测：input 撑满 27px 容器后下越界复发 → 结论：UXP 原生 number 绘制区恒高于 CSS 盒（CSS 高度压不掉原生绘制高度），input 撑满容器时原生溢出必然越出容器底。
- 定稿（完全复刻激活码 `.license-input-wrap` 比例）：容器 27px + input 19px（上下各 4px 缓冲吃掉原生溢出）+ `line-height:19px` + `display:block` + flex align-items:center。主面板/工具箱两套同步。
- MEMORY.md 第 42 条旧结论（"input 撑满容器 27px"）已更正为缓冲方案。

**验证**：tsc 改动文件无新增报错；生产构建通过；grep 确认 bundle 含 `margin-top: -9px`、`fill-mode-group .radio-item .hotkey-icon-button`、`height: 19px`×2、`new Set`，`dupIdx` 已 0 残留。

---

## 第十一轮：number-input 全面统一（32px 容器 + 24px 缓冲）+ 描边排版修复 + 开关持久化修复（18:30）

**用户结论**：齿轮/radio OK；number-input 上轮「漏掉子面板与工具箱」且描边面板排版崩坏（截图：单位符号叠到输入框）；新发现自动关开关/自动切套索不持久化。

**① number-input 终版定稿（容器 32px + input 24px 缓冲，全部 23 处覆盖）**
- 用户指令：整行/容器回到 32px、number-input 27px。落地：容器（视觉输入框）32px；input CSS 高度 24px——**input 的 CSS 高度不改变原生绘制大小，只决定缓冲量**，24px 保留已验证的上下 4px 缓冲；27px input 只剩 2.5px 缓冲，越界会复发，故取 24（视觉无差异）。
- 覆盖全部落点：主面板 #app 5 处、StrokeSetting 2 处、工具箱 AdjustmentPanel 13 处（上轮已包）、**纯色 ColorSettingsPanel 1 处 + 图案 PatternPicker 2 处 + 渐变 GradientPicker 2 处（本轮新包 `.num-input-row`）**。
- `#pixeladjustment .num-input-row` 内层选择器从 `.adjustment-number-input` 改为 `input[type="number"]`——子面板输入无类名，按类型命中。

**② 描边面板排版崩坏根因**：stroke.css `.stroke-wide-container span{margin:-3px 0 0 -5px}` 是旧结构（单位紧贴裸输入框）的补正，单位移出容器后会把 px/% 拉得偏上偏左叠到输入框。新增 `.stroke-wide-container .num-unit{margin:0 0 0 4px;display:flex;align-items:center}` 覆盖。

**③ 自动关开关/自动切套索不持久化根因（启动竞态覆盖）**：
- 时序：componentDidMount 里 `subscribeMainToggle`（250ms 轮询共享文件）先回触 `setState({isEnabled})` → componentDidUpdate 的「watchedKeys 有变即保存」用**当前默认值整体写入 panel-state.json** → 真正的 `PanelStateManager.initialize`（排在多个 await 之后、>400ms）读到的是被覆盖后的文件 → 用户已存 true 被冲回默认 false。实测磁盘文件 appPanel 几乎全为默认值，佐证。
- 修复：加 `panelStateLoaded` 门闩（load 的 finally 置 true；componentDidUpdate 保存前检查，未加载完不保存）。与 AdjustmentPanel 已有的 `panelStateLoaded` 门闩同一模式。
- 附带：`onResetParameters`（复位参数）原 `...initialState` 会把 4 个选项 checkbox + 描边开关 + 新建图层一并重置，已改为保留（选项≠参数）。

**验证**：tsc 改动文件无新增报错（app.tsx TS2304 'this' 经 stash 对照确认为 HEAD 预存在）；生产构建通过；grep bundle 确认 32px×2、`.stroke-wide-container .num-unit` 覆盖、panelStateLoaded 门闩在包。

---

## 工具箱滑块单位符号容器对齐主面板 .num-unit 样式
- 工具箱 `.adjustment-unit`（`src/adjustments/adjustment.css`）原先带 `opacity:0.8` + 固定 `width:14px;flex:0 0 14px` + `text-align:left`，与主面板 `.num-unit`（`styles.css`）观感不一致（单位符号偏暗一截）。
- 对齐为：去掉 `opacity:0.8`、改 `flex:0 0 auto`（内容宽度，与 `.num-unit` 一致）、删 `width:14px`/`text-align:left`；保留 `color/font-size:13px/white-space:nowrap/display:flex/align-items:center`。
- 与数字输入的间距由父级 `.unit-container` 的 `gap:3px` 提供，与 `.num-unit` 的 `margin-left:4px` 同源；**未**在 `.adjustment-unit` 上重复加 `margin-left`，避免与 gap 叠加成 7px 破坏行内对称（slider→input 也是 3px）。
- 仅改 CSS，无需改 JSX；改后需 `node ./node_modules/webpack/bin/webpack.js --mode=production` 重新构建才生效。

---

## 十二轮：内联样式收口到 CSS + 冗余 CSS 清理 + 激活面板样式独立拆文件（22:22）

**激活面板样式拆分（用户指定）**
- 新建 `src/styles/license.css`，搬运原 `LicenseDialog.tsx` 内 `<style>` 块全部规则；`LicenseDialog.tsx` 删除 `<style>`，改由 `src/index.tsx` 在 `styles.css` 之后 `import './styles/license.css'`（保留「弹窗样式排在主面板样式之后」层叠顺序；遮罩背景色仍由 theme.ts 提供）。
- 加载机制确认：UXP 实际样式来自 `bundle.js` 经 style-loader 注入（css-loader 合并 @import）；`dist/index.html` 的 `<link styles.css>` 是原始拷贝、其 @import 兄弟文件未随包拷贝，不影响。构建已验证 bundle.js 含 `license-dialog`/`license-btn`。

**内联样式迁移（APP 面板 + 四子面板 + 工具箱，仅搬「静态生效」样式；运行时计算的动态样式保留内联）**
- 主面板 `index.tsx` 两面板根 `<div>` 内联布局 → `.app-root`/`.toolbox-root`（styles.css/adjustment.css）。
- 工具箱 `AdjustmentPanel.tsx`：剔除冗余内联——`.adjustment-swtich-label` 在 CSS 已有 `cursor:pointer` 故删 4 处内联 cursor；开关 `marginLeft:8px` 收口为 `.adjustment-swtich-container > sp-switch`；独立左对齐开关收口为 `.adjustment-swtich-align-left`；`mask-sync-footer` 图标按钮 `marginLeft:4` 收口为 `.mask-sync-footer .hotkey-icon-button`；反相 label 的 `color:var(--text-color)` 冗余删除。
- 渐变 `GradientPicker.tsx`：四处 `midpoint-slider` 仅 `left` 动态，静态外观收口为 `.midpoint-slider`（gradient.css）；背景渐变/滑块位置等运行时计算保留内联。
- 图标组件 `Icons.tsx` 增加 `className` 透传，三面板图标 `display:block` 尺寸内联 → `.icon-14/15/16`；`BrushSelect` 增加 `className` 透传。
- `POP_LAYER_STYLE`（原内联于 Select/MaskSyncSelect 弹层）的静态 `transform:translateZ(0);will-change:transform` 收口进 `.mask-sync-select-pop`，弹层仅留动态 `pos.left/top/width` 内联；`POP_LAYER_STYLE` 常量从 `popOverlay.ts` 与两调用处一并删除（orphan 清理）。

**冗余 CSS 清理（脚本扫 selector × 全源码类名引用，0 引用者删除；模板插值类 `slider-text-${n}`/`hotkey-circle-button${...}`/`license-input-wrap${...}` 等为误报，保留）**
- styles.css：删 `.stroke-settings-icon*`、`.settings-icon*`。
- gradient.css：删 `.color-picker-container`。
- pattern.css：删 `.zoom-select`/`.zoom-select:focus`。
- stroke.css：删 `.stroke-subtitle`/`.stroke-subtitle h3`、`.position-radio-item-label`。
- adjustment.css：删 `.adjustment-select`、`.adjustment-button-wide`、`.adjustment-button-6`（5/7/8 精确档保留）、`.mask-sync-row-wrap`、`.hotkey-inline-hint`、`.hotkey-empty-hint`、`.hotkey-entry-actions-right`；按钮宽度分段注释同步修正。

**验证**：生产构建通过（仅 bundle/字体/exe 体积 warning）；静态内联样式经 grep 确认目标面板仅剩运行时动态值；`tsconfig.noUnusedLocals` 未开，删 import 无碍。构建命令：`node ./node_modules/webpack/bin/webpack.js --mode=production`。

## 十二轮补：POP_LAYER_STYLE 清理不彻底导致运行时崩溃（23:43）
**现象**：点开笔刷下拉菜单报 `ReferenceError: POP_LAYER_STYLE is not defined`（react-dom 开发构建）。
**根因**：十二轮删 `popOverlay.ts` 的 `POP_LAYER_STYLE` 导出 + 清 `Select.tsx` 的 import 时，**`Select.tsx` 第 183 行的用法 `...POP_LAYER_STYLE` 没同步删掉**（编辑未落地），运行时该标识符已不存在 → 崩溃。CSS 侧 `.mask-sync-select-pop` 已含 `z-index:99998`/`transform:translateZ(0)`/`will-change:transform` 三项静态值，故删用法后样式无回退。
**修复**：`Select.tsx` 第 183 行 `style={{ left, top, width, ...POP_LAYER_STYLE }}` → `style={{ left, top, width }}`；重新生产构建。验证：`src/` 仅剩 CSS 注释提及 `POP_LAYER_STYLE`，`dist/bundle.js` 再无代码引用（仅 CSS 注释字符串）。
**教训**：删导出常量时，必须对所有 import/usage 做全量 grep 复核，不能只删 import 行——漏删的用法不会在构建期报错（开发构建下才炸）。
