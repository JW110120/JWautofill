# 2026-09-04

## app.css → common.css 收口（按 app.css 批注）

按用户在 app.css 每个样式上的批注，继续将通用样式收口到 common.css 并改相应 TSX：

- 新增通用类（common.css）：`.label-disabled`（仅改色，替代原 `.app-blendmode-label-disabled`）、`.row-start` / `.row-end`（左/右对齐行容器，替代 `.stroke-color-group` 与 fill-mode 的 margin hack）、`.color-preview`（尺寸/光标/圆角，与渐变预览共用；描边无边框、渐变 1px 边框由各自面板 CSS 修饰层补充）、`.copyright`。
- `.radio-item` 加 `position: relative`：保证 fill-mode 内绝对定位的齿轮图标解析到本 radio 行（而非更外层定位祖先），是本次 fill-mode-group 挂 panel-section 后的必要保护。
- app.tsx 改动：blendmode 禁用态 `'app-blendmode-label label-disabled'`；描边组 `row-start`、预览 `color-preview stroke-color-preview`；fill-mode 区块 `panel-section fill-mode-group`，三个 radio 的 label+齿轮包进 `.row-end`；info 条 `panel-section` 并在其内补 `.divider` 取代原上描边。
- app.css 删除：`.app-blendmode-label-disabled`、`.stroke-color-group`、`.stroke-color-preview` 全量（改修饰）、`.fill-mode-group` 基础块（改挂 panel-section）、`.fill-mode-group .radio-item-label` 错误 margin hack、`.info-plane`、`.copyright`。保留的 APP 专属：`app-root` / `body.secondary-panel-open` / `app-blendmode-container` / `app-blendmode-label` / `slider-container` / `.fill-mode-group .radio-item .icon-button`（齿轮绝对定位，⚠️必要）/ `.fill-mode-group sp-radio`（覆盖通用双列 radio，⚠️必要）。
- gradient.css 的 `.color-preview` 改为只补 `border + border-radius:2px` 的修饰层（尺寸/光标走 common）。

注：未跑 webpack 构建验证（仅 className 字符串与包裹 div 改动，无类型变化）；UXP 实机外观需人工核对。

## 第二轮收口（用户 6 条补丁）

1. color-preview 全收口：common.css 的 `.color-preview` 统一加 `border:1px solid var(--border-color)`、`border-radius:3`、`margin:0 10px`；删除 app.css `.stroke-color-preview` 与 gradient.css `.color-preview` 修饰规则；app.tsx 描边预览改纯 `color-preview`。
2. fill-mode-group 彻底移除：app.tsx 的 `panel-section fill-mode-group` → `panel-section`，三行纵向布局改由 `sp-radio-group` 挂通用 `radio-group--vertical`（common.css 新增 `.panel-section .radio-group--vertical` 规则）；齿轮绝对定位改通用 `.radio-item .icon-button`（app.css）。
3. 修复三行 radio 重合：撤销三个 radio 内错误的 `.row-end` 包裹，恢复 label + IconButton 为 radio-item 直接子元素（齿轮绝对定位于行右）。`.row-end` 类从 common.css 删除（已无用法）。
4. switch-container 去掉 `border-bottom`，app.tsx 三个开关后各补一个 `<div className="divider" />`（共 3 个）。
5. radio-group-label 收口：common.css 删规则，app.tsx 填充模式标题改 `label-4`。
6. collapse-section 的 title 挂 label-N：app.tsx 两处折叠标题 `选区选项`/`填充选项` 与 AdjustmentPanel.tsx 的 `{section.title}` 均挂 `label-4`（均为 4 字）。

⚠️ 环境坑：本会话 Edit 工具对多文件、尤其长 old_string 的写入大面积「假成功不落盘」（disk 字节未变）。最终改用 Python 直接读写文件才可靠落地。后续在本仓库做类似多文件重构时，优先用 Python 脚本改文件，不要迷信 Edit 的 "Successfully edited"。

UXP 实机需人工核对：① 三行 radio 齿轮是否仍贴本行右侧且不压标签；② 渐变/描边色块因统一 margin:0 10px + 1px 边框后的间距观感；③ 三个开关分隔线（divider 15px 上下边距）与原 border-bottom 的间距差异。

## 三行 radio 重合最终修复（row-end 文档流方案）

第 2 轮第 3 条的"恢复绝对定位齿轮"方案实机重合（根因：sp-radio 影子布局把标签排到行最右，绝对定位齿轮 pin 在 right:0 与其必然叠在同一点；旧 margin-right:28px 只是手工让位）。最终方案：
- 齿轮彻底废除绝对定位，与标签一起包进 `.row-end`（common.css 重新加回该类），处于正常文档流；水平由 `.radio-item` 的 space-between（圆点左、row-end 组右）+ row-end 的 flex-end 排布，垂直居中由 row-end 的 align-items:center 保证。
- app.css 齿轮规则改为 `.radio-item .row-end .icon-button { margin-left: 8px }`（仅给标签与齿轮间距）。
- **经验**：UXP 的 sp-radio 内部会把 slot 内容排到行右侧，凡 sp-radio 内放自绘元素，绝不要用绝对定位 pin 边缘；用「外层 space-between + 内层 row-end/flex-end 文档流排布」。

## pattern.css 逐条收口（按 pattern.css 批注）

按 pattern.css 每条批注收口到 common.css 并改 TSX（全部 python 脚本落地，已校验无残留类）：
- 预设区：`.pattern-container` + 渐变 `.gradient-presets-area` → 通用 `.preset-area`（common，高 130px / z-index:1）。`GradientPicker` 改挂 `preset-area`。
- `.pattern-preset` 保留并修滚动 bug：旧 `height:auto` 在定高 flex 列里无法触发内部滚动→改 `flex:1 1 auto; min-height:0`，滚动条归其自身；底部图标栏经 `.icon-button-group--bar` 的 `margin-top:auto` 顶到底。
- 缩略图盒去重：`.photo-container` / `-selected` / `-multi-selected` 公共外观抽成 common `.thumb-box`（74px 盒 + flex 居中）；选中/多选仅在 pattern.css 留「描边色/宽 + 尺寸」修饰。TSX 默认 `thumb-box`，选中 `thumb-box photo-container-selected`，多选 `thumb-box photo-container-multi-selected`。
- 图片规则重合分析：原 `.photo-container img`(width/height:100%) 与 `.pattern-thumb-img`(width/height:auto+padding) 命中同一 `<img class="pattern-thumb-img">`，前者被后者在特异性上覆盖=死代码；合并为唯一 `.thumb-box img`（auto + max 100% + padding:4px + object-fit:contain）。
- `.pattern-thumb-loading` 收口 common（token `--black-text-` 已定义，保留）。
- `.icon-button-group--bar`：pattern(绝对) + gradient(relative) 合并为 common 通用条（height:32px + border-top + margin-top:auto + flex row 右对齐）；删 pattern/gradient 两份副本。
- `.pattern-settings-area` + 工具箱 `.adjustment-section` → 通用 `.border-panel-section`（common，padding10+border+radius，类 panel 仅多描边）。`AdjustmentPanel` 3 处改挂。`adjustment.css` 删原名。
- 删 `.pattern-setting-item-group` / `.pattern-setting-item` / `.pattern-fillmode-container`：slider-block 与 radio 直接进 `border-panel-section`；补两条 divider（滑块↔radio、radio↔checkbox）。
- `.pattern-final-preview-container` + 渐变 `.final-preview-container` → 通用 `.final-preview-container`（common，高 120px）。`GradientPicker` 已用该类。
- `.preview-wrapper` 收口 common。删 `.preview-controls`（`pattern-subtitle` 改用 `.row-between`，`h3` 改用 `.subpanel-title-2`）。
- common 两列 radio 选择器 `.panel-section/.pattern-fillmode-container sp-radio*` 改为 `.panel-section/.border-panel-section sp-radio*`（退役 pattern-fillmode-container，使 border-panel-section 内双列 radio 仍生效）。

⚠️ 待 UXP 实机核对：① 预设区滚动条是否归在列表内（非占满整区）；② 最终预览区 `final-preview-container` 高 120px 但内部 `.preview-wrapper` 高 200px 会被 `overflow:hidden` 裁掉底部 80px——此高度矛盾是批注里同时写死的两个值所致，需确认是否要调 `.preview-wrapper` 高度；③ 图标栏统一右对齐（原 gradient 为左对齐）；④ h3 由 14px 变 subpanel-title-2 的 16px。

## pattern-preset 四格等距布局（修正）

`thumb-box` 原 56px 在 4 格下必换行，根因是可用内宽远小于 4×56：

- **面板宽度链（实测，纠正记忆里"面板 248"）**：manifest.json 图案面板 `width:250` → `.panel`(`box-sizing:border-box; padding:10`)= 内 230 → `.preset-area`(直接子、border-box、border1×2)= **内 228**。
- `.thumb-box` 本就 `box-sizing:border-box`，56px 即含 1px 描边的外尺寸；4×56=224 已几乎吃满 228，无空间放 5 段间距。
- **等距公式**（左右边缘间距=格间距=M，容器 border-box 使 `4×B + 5×M = 228`）。整数解仅 (52,4)/(47,8)/(42,12)。取最贴近 56 的 **B=52、M=4**：4×52+5×4=228 整除、零换行零余量。
- 改动：common.css `.thumb-box` 56→52；pattern.css `.pattern-preset` 加 `box-sizing:border-box`、`padding:0 4px`，并保留 `.pattern-preset .thumb-box{margin-right:4px}` / `:last-child{margin-right:0}`。gradient.css 的 35px 盒是别类、不受影响。
- ⚠️ 之前记忆称"面板宽 248"有误，实为 250；后续任何面板内部件等距/整除排版都按 228 内宽（图案面板）起算。
